# 🎨 نظام التصميم (Design System) — الاستايل النهائيّ المعتمَد

> **الغرض:** هذا الملف **مرجع الأسلوب البصريّ فقط** (ألوان/خطوط/مساحات/كروت/حركات) — **ليس المحتوى**. نرجع إليه عند بناء الصفحات النهائيّة، فيُطبَّق **المحتوى المُتّفق عليه في الدستور** بهذا الاستايل.
> **المصدر:** مُستخرَج من 3 صفحات HTML مرجعيّة (profile / wallet / settings). **اسم العلامة «HalaCareer» ونطاقها في المرجع مُتجاهَلان** — الهويّة النهائيّة لنا.
> **العلاقة بالدستور:** يُكمّل «الاتجاه البصريّ» (قسم 2.10 / 2.10.1) ويُحوّله إلى **قيمٍ دقيقة قابلة للبناء**.
> **الحالة:** ✅ **معتمَد نهائيًّا** (بأمر المالك).
> **استعراض حيّ:** `نظام-التصميم-preview.html` (عرض تفاعليّ لكلّ المكوّنات بالوضعين الداكن/الفاتح).
> **آخر تحديث:** 2026-07-23

---

## 1. القاعدة العامّة
- **الوضع الافتراضيّ: داكن (Dark)** مع دعم كامل لوضع **فاتح (Light)** عبر `html.light`.
- **RTL/LTR:** عربيّ = RTL بخطّ **Cairo** لكلّ العناصر؛ إنجليزيّ = LTR.
- **مبدأ اللون الواحد (Teal-only):** كل درجات الإبراز **تركوازيّة**. المرجع **يُجبِر** أيّ لون آخر (أحمر/أصفر/بنفسجيّ/أخضر/أزرق) على التحوّل للتركوازيّ. الاستثناء العمليّ الوحيد: **حالات سلبيّة صريحة** (مثل «لم يكمل التسجيل») يجوز فيها أحمر خافت — نقرّرها لكلّ حالة.
- **إحساس عامّ:** هادئ، احترافيّ، عميق، بلمسات توهّج تركوازيّ خفيفة (Glow) وملمس ضوضاء ناعم.

## 2. الخطوط (Typography)
| الاستخدام | الخطّ | ملاحظات |
|---|---|---|
| العناوين (LTR) | **Bricolage Grotesque** | 400/600/700/800، `line-height:1.1` |
| النصّ (LTR) | **Plus Jakarta Sans** | 300–700، `line-height:1.65` |
| **كلّ العربيّ (RTL)** | **Cairo** | 400/500/600/700/800 — يشمل العناوين والأزرار والحقول |
- تنعيم الخطّ: `-webkit-font-smoothing:antialiased`.
- Eyebrow/تسمية علويّة: `.65rem`، `font-weight:700`، `letter-spacing:.18em`، uppercase، لون `--td`.

## 3. لوحة الألوان — Tokens (متغيّرات CSS)
### الوضع الداكن (الافتراضيّ)
```
--bg:  #030d17     /* الخلفيّة الأساسيّة */
--bg2: #04111e     /* خلفيّة ثانويّة/مودال */
--bg3: #071825     /* خلفيّة ثالثة */

--t:   #00d4b8     /* التركوازيّ الأساسيّ (Primary) */
--tl:  #2de0ca     /* تركوازيّ فاتح (Light/Hover) */
--td:  #009e85     /* تركوازيّ غامق (Dark/Eyebrow) */

/* شفافيّات التركوازيّ rgba(0,212,184,x) */
--ta30:.30  --ta20:.20  --ta15:.15  --ta12:.12  --ta08:.08

/* الأبيض rgba(255,255,255,x) */
--w:#fff  --w80:.80  --w60:.60  --w40:.40  --w20:.20  --w10:.10  --w06:.06
```
### الوضع الفاتح (`html.light`)
```
--bg/--bg2/--bg3: #ffffff
--t:#009e85  --tl:#00b89e  --td:#007a68
--w:#0a2220 (نصّ داكن) وتدرّجاته بنفس النِّسَب
/* في الوضع الفاتح: كل الكروت الداكنة → #ffffff، والحدود rgba(0,212,184,.2)، وتُلغى الظلال والشبكات والضوضاء */
```
- **تدرّج الإبراز الأساسيّ (Gradient):** `linear-gradient(135deg,#2de0ca 0%,#00d4b8 55%,#009e85 100%)` — يُستخدم للنصوص المميّزة (`background-clip:text`) والأزرار وأشرطة التقدّم.

## 4. الخلفيّات والملمس
- **ملمس ضوضاء (Noise):** طبقة SVG `fractalNoise` ثابتة فوق الصفحة بـ `opacity:.04` (تُلغى في الفاتح).
- **شبكة نقاط (dotgrid):** `radial-gradient(rgba(0,212,184,.07) 1px)` كل **32px**.
- **شبكة خطوط (linegrid):** خطوط `rgba(0,212,184,.05)` كل **64px**.
- **زجاج (Glass):** `background:rgba(5,17,30,.8)` + `border:1px solid --ta20` + `backdrop-filter:blur(20px)`.
- **غلاف/كوفر (Cover):** تدرّج غامق + وهجان راديال تركوازيّان + شبكة نقاط + تلاشٍ سفليّ نحو `--bg2`.

## 5. الزوايا والمساحات
- **نصف القطر (Radius):** الحقول/الأزرار `~.72–.75rem` · الكروت `1–1.1rem` · المودالات `1.25rem` · الحبوب/الشارات `999px` (Pill) · الأيقونات المربّعة `.5–.85rem`.
- **الحاوية القصوى:** `max-width:1280px` مع `padding` جانبيّ `1.25rem`.
- **الانتقالات (Transitions):** `.15s–.28s`؛ منحنى النعومة المفضّل `cubic-bezier(.22,1,.36,1)`.

## 6. الكروت (Cards)
- **الأساس:** `radius:1rem`، `border:1px solid --ta15`، `background:rgba(5,14,26,.85)`، `overflow:hidden`.
- **Hover:** رفع لون الحدّ إلى `rgba(0,212,184,.25)`؛ بعض الكروت ترتفع `translateY(-2px)` مع ظلّ `0 12px 36px rgba(0,80,70,.18)`.
- **كارت الإعدادات (scard):** `radius:1.1rem`، رأس (`scard-head`: عنوان + أيقونة `34px`) + جسم (`scard-body`).
- **صفّ مميّز (feat):** حدّ تركوازيّ أوضح + شريط جانبيّ `3px` متدرّج + خلفيّة متدرّجة خفيفة.

## 7. الأزرار (Buttons)
- **أساسيّ (btn-p):** خلفيّة `linear-gradient(135deg,#2de0ca,#009e85)`، نصّ داكن `#020e18`، `font-weight:700`، `radius:~.72rem`، Hover: `opacity:.9` + `scale(1.01–1.015)`.
- **ثانويّ/شبح (btn-g / btn-outline):** `border:1px solid --ta20`، نصّ `--w80`، خلفيّة `--ta08`، Hover: حدّ `--ta30` + خلفيّة `--ta12`.

## 8. الحقول والقوائم (Inputs / Selects)
- **إدخال (hc-input):** `background:rgba(5,14,26,.6–.75)`، `border:1.5px solid --ta15`، `radius:~.72rem`، `padding:.68rem 1rem`، Focus: حدّ `--t`. Placeholder `--w40`.
- **قائمة (hc-select):** نفس النمط + سهم SVG مخصّص (شيفرون تركوازيّ)؛ في RTL يُنقل السهم لليسار.
- **Textarea:** نفس نمط الإدخال مع `resize:vertical`.
- **حقل كود الهاتف (phone-code):** عرض `110px` بجانب حقل الرقم.
- **تسمية الحقل (field-label):** `.76rem`، `font-weight:700`، لون `--w60`.
- **شبكة الحقول:** عمودان `1fr 1fr` (يصبح عمودًا واحدًا تحت `600px`).

## 9. الرقاقات (Chips) والفلاتر
- Pill: `padding:.3rem .85rem`، `radius:999px`، `.75rem`، `font-weight:600`، لون `--w60`، حدّ `--ta15`.
- الحالة النشطة/Hover: خلفيّة `--ta12` + حدّ `--ta30` + نصّ `--tl`.

## 10. السويتش (Toggle)
- المسار (track): `40×22px`، `radius:999px`، خلفيّة `rgba(255,255,255,.15)`؛ عند التفعيل → `--t`.
- الإبهام (thumb): دائرة `16px` بيضاء تنزلق من `3px` إلى `21px` (يحترم RTL عبر `inset-inline-start`).

## 11. المنزلقات (Range Sliders)
- `input[type=range]` بلون إبراز **تركوازيّ** `accent-color:#00d4b8`.
- تُستخدم في الآلة الحاسبة التفاعليّة (صفحة الدعوات) وحجم الخطّ في الإعدادات (12–20px مثلًا).
- **مطلوب:** استجابة **لحظيّة** لقيمة المنزلق (عرض الرقم يتغيّر فورًا) — التفاعل جوهر التصميم لا كماليّ.

## 12. أشرطة التقدّم (Progress Bars)
- ارتفاع `4–6px`، `radius:999px`، مسار `--ta12`، تعبئة متدرّجة `linear-gradient(90deg,#009e85,#2de0ca)`.
- انتقال العرض: `.9s–1s cubic-bezier(.22,1,.36,1)`.

## 13. التنقّل الجانبيّ للتابات (Side Nav)
- حاوية (side-nav): `background:rgba(5,14,26,.85)`، `border:1px --ta12`، `radius:1rem`، `padding:.45rem`، **Sticky** `top:80px`.
- رابط (side-link): صفّ (أيقونة `32px` + نصّ `.88rem/700`)، لون `--w60`؛ **النشط** يبرز بخلفيّة أيقونة `--ta15` ولون تركوازيّ.
- **الخروج** يُفصَل أسفل القائمة (فاصل بصريّ).

## 14. الأشرطة العلويّة العائمة (Sticky Bars)
- **شريط تنقّل داخليّ (snav):** Sticky `top:64px`، خلفيّة `rgba(3,13,23,.96)` + `blur(20px)`، تبويبات بحدّ سفليّ متحرّك عند النشط.
- **شريط الـ CV العائم (cv-sticky-bar):** يبقى أعلى تاب السيرة **وينزل مع الاسكرول** (يمينًا العنوان، يسارًا زر «حفظ CV») — نفس خامة الزجاج/الكارت.
- **شريط الصفحة (page-strip):** خلفيّة `--bg2` بحدّ سفليّ خفيف.

## 15. صفوف الجلسات والشارات (Sessions / Badges)
- صفّ الجلسة (session-row): أيقونة `36px` + معلومات + شارة، بفاصل سفليّ `--ta08`.
- شارة (session-badge): `.62rem`، `font-weight:700`، `padding:.2rem .65rem`، `radius:999px` بحدّ ملوّن حسب الحالة.

## 16. الأفاتار (Avatar)
- حلقة متدرّجة مخروطيّة `conic-gradient(#2de0ca,#009e85,#2de0ca)` حول الصورة، مع هالة `0 0 0 4px rgba(0,212,184,.12)`.
- نقطة حالة نابضة (`pulse-dot`) لو أونلاين.
- في الإعدادات: دائرة `88px` بحدّ `--ta20` + زرّ كاميرا صغير للتغيير.

## 17. النوافذ المنبثقة (Modals)
- غطاء (mwrap): `rgba(0,0,0,.75)` + `blur(16px)`.
- الصندوق (mbox): خلفيّة `--bg2`، `border:1px --ta20`، `radius:1.25rem`، `max-width:480px`، ظهور بحركة `fadeup`، ظلّ عميق.

## 18. المؤشّر المخصّص (Custom Cursor) — أجهزة الفأرة فقط
- نقطة `8px` تركوازيّة تكبر إلى `42px` (شفّافة بحدّ) فوق العناصر التفاعليّة، بانتقال `cubic-bezier(.22,1,.36,1)`. تُخفى على اللمس.

## 19. القوائم المنسدلة (Nav Dropdowns)
- لوحة `min-width:220px`، `radius:1rem`، خلفيّة `rgba(2,8,18,.97)` + `blur(20px)`، ظهور بانزلاق `translateY(-6px)→0`.
- عنصر: أيقونة مربّعة `28px` (خلفيّة `--ta08`) + نصّ `.82rem`؛ Hover خلفيّة `--ta08`، شيفرون يدور 180° عند الفتح.

## 20. الحركات (Animations)
| الاسم | المدّة/المنحنى | الاستخدام |
|---|---|---|
| `fadeup` | .55s ease | ظهور العناصر (16px للأعلى) |
| `float` | 5s ease-in-out | طفوّ خفيف (−4px) |
| `text-shimmer` / `shimmer` | 5s / 3s linear | لمعان النصوص المتدرّجة |
| `pulse-dot` | 2s | نبض نقطة الحالة |
| `spin-slow` | 11s linear | دوران بطيء (شعار/زخرفة) |
| `ticker` | 22s linear | شريط متحرّك |
| `blink` | 2s | وميض تنبيهيّ |

## 21. الأيقونات (Icons)
- نمط **خطّي (Stroke)** بأسلوب Feather: `fill:none`، `stroke:currentColor`، `stroke-width:2–2.5`، `stroke-linecap/linejoin:round`.
- ألوانها تتبع النصّ (تركوازيّ غالبًا). أحجام شائعة `14–18px` داخل حاويات مربّعة بخلفيّة `--ta08`.

## 22. الشعار (Logo)
- في المرجع: شعار **أقواس متّحدة المركز** (شكل Roadmap/طبقات) بتدرّج تركوازيّ — **متوافق مع اتجاه «الرودماب» في الدستور (2.10)**.
- **الشعار النهائيّ يرفعه المالك**؛ ما هنا **Placeholder** للأسلوب فقط.

## 23. شريط التمرير (Scrollbar)
- عرض `5px`، مقبض `--ta20`، مسار `--bg`، `radius:3px`.

---

### ملاحظات التطبيق
1. المرجع يستخدم **Tailwind (CDN)** + **Google Fonts** + أنماط مضمّنة — عند البناء النهائيّ نلتزم بستاك الدستور (**PHP Native + Tailwind + JS**)، ونستنسخ **القيم أعلاه** لا الأكواد حرفيًّا.
2. **التوافق الإلزاميّ:** الوضعان (داكن/فاتح) + RTL/EN معًا في كلّ مكوّن — كما في المرجع.
3. أرقام/حالات الصفحات المرجعيّة (أسماء، أسعار، جلسات…) **محتوًى وهميّ** — المحتوى الفعليّ من الدستور فقط.
4. **قاعدة الوضع الفاتح:** يُختبَر إلزاميًّا لكلّ مكوّن. **لا تُطبَّق تأثيرات زخرفيّة (`opacity:0`) على حاويات بنيويّة** (مثل `body`) — تُزال الزخرفة بإلغاء `background-image` لا بتعتيم العنصر، وإلا اختفى المحتوى كلّه (صفحة بيضاء).
