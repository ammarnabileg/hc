<?php

use App\Http\Controllers\Trainee\ReferralController;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| بوّابة الدعوة العامّة (7.6 · 7.6.2)
|--------------------------------------------------------------------------
| ⭐ **حسم تعارض داخل الدستور:** 7.6 يكتب صيغة الرابط `?offer=<user_id>`
| بينما 7.6.2 يكتبها `/join?ref=CODE`. المعتمَد هنا صيغة **7.6.2** لأنّها
| لا تكشف المعرّفات الرقميّة (العدّ التسلسليّ يُفشي حجم القاعدة ويسمح
| بتخمين الروابط)، **مع إبقاء `?offer=` مقبولًا للتوافق** فلا تموت روابط
| قديمة بين يدي الناس.
|
| والبوّابة بلا `auth` عمدًا: هي أوّل ما يفتحه ضيفٌ لم يسجّل بعد، ووظيفتها
| تثبيت الداعي في **السيشن** ثمّ إرساله للتسجيل.
*/

Route::get('/join', [ReferralController::class, 'join'])->name('referral.join');
