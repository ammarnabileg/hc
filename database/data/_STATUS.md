# 🗂️ `database/data` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** «data» ضمن: المخطّط والبذور والمصانع.
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (1):**
  - `flags.php`
- **ملفّات أخرى (2):** `countries.json` · `permissions.json`
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**أوديت المرحلة 5 (12.2.2) — مطابقة `permissions.json` بالمصفوفة صفًّا صفًّا:**

- ✅ **المفاتيح:** 1033 مفتاحًا فريدًا في الملفّ = 1033 في صفوف المصفوفة بالضبط.
  **لا مفتاح ناقص ولا مفتاح زائد بلا سند.**
- ✅ **النطاقات:** صفر مخالفة — `allowed_scopes` لكلّ مفتاح = عمود «النطاقات» في صفّه.
- ✅ **العزل:** 89 مفتاحًا `is_owner_only` = 89 صفًّا نصّه «مالك المنصّة فقط».
- ✅ **الشروط:** صفر نصّ شرطٍ بلا مفتاح مقفول في `ConditionMap`.

⬜ **الفجوات المتبقّية في الملفّ:**
1. **54 وسم 🔒 زائد بلا سند في المصفوفة** — `is_sensitive=1` على 51 مفتاحًا
   صفّها في الدستور بلا 🔒 وبلا «مالك المنصّة فقط»: `roles.assign` · `impersonation.create` ·
   `user_deletion.delete` · `rep_manual.*` (5) · `vxp_manual.*` (2) · `investigations.*` (6) ·
   `admin_notes.*` (4) · `maintenance.*` (3) · `updates.*` (5) · `backups.*` (7) ·
   `integrations.*` (5) · `audit_logs.*` (3) · `webhooks.*` (6) · `setup_installer.*` (2).
   المصفوفة هي المرجع، والوسم الزائد يخالفها.
2. **✅ [أُغلِقت 2026-09-11] `refunds.view` و`refunds.edit` — شرط العزل المُقحَم رُفِع.**
   المصفوفة تقول الشرط «**دائمًا**» (و🔒 في ترويسة 12.2.2 «= صلاحيّة **حسّاسة**» لا عزلًا،
   والعزل نصُّه «مالك المنصّة فقط» ⟵ `is_owner_only`)، وكان الملفّ يكتب للصفّين
   `conditions: ["مالك المنصّة فقط"]` و`condition_keys: ["platform_owner"]` **بلا** وسم عزل —
   وهو الصفّان **الوحيدان** في الملفّ كلّه بهذا الهجين. وأثره كان أسوأ الوجهين معًا: الصفّ
   **يُمنَح** لدور «المسؤول الماليّ» (لأنّ `grantResources` تستثني `is_owner_only` وحده، و12.2.3-أ-7
   تنصّ على `… · refunds · …` بنطاق ALL)، ثمّ **يسقط عند التقييم** لأنّ `platform_owner` خارج
   `ConditionEvaluator::TARGET_BOUND` فتُقاس بلا هدفٍ وتردّ `isPlatformOwner()` — أي **صفٌّ ميّت
   في `permission_role`**. صار الصفّان `conditions: []` و`condition_keys: []` كنصّ «دائمًا»،
   و`is_sensitive: true` (🔒) و`is_owner_only: false` كما هما.
   **قياسٌ قبل/بعد على القاعدة المزروعة:** `finance_admin->allows('refunds.view')` كانت **false**
   والصفّ `refunds.view@ALL/allow` مزروعٌ فعلًا، وصارت **true**. و`platform_owner` **true** في
   الحالتين، و`trainee` **false** في الحالتين.
   **ولم يُفتَح بابٌ ماليّ:** شاشة 🔒 الماليّات محروسة بـ`finance.view`/`finance.edit` وهما
   معزولتان بنصّ صفّيهما، والمسؤول الماليّ يبقى خارجها (403) — ولا مسارَ في المستودع يحرسه
   `refunds.*` أصلًا، فالمفتاحان قدرةٌ في المصفوفة بانتظار شاشتها.
   **الدليل:** `PermissionArchitectureTest` — ثلاثة اختبارات:
   `test_the_finance_admin_actually_holds_the_refunds_resource` ·
   `test_the_platform_owner_condition_never_stands_without_the_isolation_flag` (يقفل عودة الهجين) ·
   `test_the_protected_finance_screen_stays_shut_for_the_finance_admin`.
3. **5 أفعال خارج الأفعال القياسيّة الثلاثة عشر** (12.2.1-أ):
   `articles.review` · `articles.publish` · `image_templates.publish` · `image_templates.batch` ·
   `image_export.use`. (المصفوفة نفسها تُشير إليها ⚠️ بانتظار قرار توحيدها.)
4. **فارق العدّ عن ترويسة 12.2.2:** الترويسة تنصّ على **1036 فريدة / 1051 صفًّا / 206 حسّاسة /
   92 معزولة**، وصفوف المصفوفة المعدودة **1033 / 1048 / 152 / 89** — والملفّ يطابق الصفوف
   لا الترويسة. فبمقياس الترويسة: **3 مفاتيح و54 وسمًا حسّاسًا و3 مفاتيح معزولة غير موجودة**.
5. **سقف النطاق غير مفروض وقت التقييم:** `allowed_scopes` تُفحَص عند الكتابة فقط
   (`PermissionExpander` · `RoleEditor`)، أمّا `ScopeResolver` فيقبل أيّ نطاقٍ من الستّة.
   وصفٌّ يدخل من طريقٍ آخر بنطاقٍ أوسع من صفّه في المصفوفة **يُحترَم** — أُثبِت بالتشغيل:
   `org_chart.view@ALL` (سقف المصفوفة TRACK) مُنِح ونفَذ.

**مُضاف في د-10 (2026-08-03) — ملفّان جديدان:**

- `countries.json` (**758KB**) — نسخة التنصيب من `dr5hn`، **مولَّدة بالآليّة** لا بيد:
  `php artisan countries:check-source --force --dump=database/data/countries.json`.
  تحمل **إسناد ODbL v1.0** داخلها، ويزرعها `CountriesSeeder` في مسار الإنتاج بلا شبكة.
  ⚠️ لا تُحرَّر بيد — أعِد توليدها من المصدر.
- `flags.php` — هندسة أعلام **249 دولة** بلغة طبقات صغيرة يقرؤها
  `App\Services\Geo\FlagLibrary` فتخرج SVG مرسومًا داخل الصفحة (2.5-ب).
  ⛔ لا رابط ولا ملفّ صورة هنا ولا في المُخرَج.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **✅ [2026-09-11] الفجوة (2) — شرط `platform_owner` المُقحَم على صفّي `refunds` — أُغلِقت.**
  التفصيل والقياس قبل/بعد في البند أعلاه.
- **الحالة:** أوديت المرحلة 5 (الأدوار والصلاحيّات) انتهى — وبقي من فجواته **أربع** (1 · 3 · 4 · 5)،
  ثلاثٌ منها (3 · 4) بانتظار قرار المالك لا إصلاحٍ تقنيّ.
- **آخر نقطة وصلنا لها:** 2026-09-11 — إغلاق الفجوة (2) بتصحيح الصفّين في الملفّ وإعادة زرعهما،
  وتثبيت الفرق سلوكيًّا باختبارات `PermissionArchitectureTest`.
- **الخطوة الجاية:** رفع الفجوات الأربع الباقية لمالك المنصّة — والفجوة (1) وحدها إصلاحُ بيانات.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `database/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-03 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-03 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
