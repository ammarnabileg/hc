<?php

use App\Services\Certificates\CertificateSignature;
use Illuminate\Database\Migrations\Migration;

/**
 * 🔏 ختمُ الشهادات القائمة مرّةً واحدة — خطُّ أساسٍ **مُعلَن** لا صمت (8.1 · 12.5-هـ).
 *
 * ابتداءً من هذه النسخة صارت صفحة التحقّق **تُعيد اشتقاق التوقيع وتقارنه**
 * (`CertificateSignature`). والحقل `hash` عاش قبلها بلا قارئ، فكُتِب بصيغٍ شتّى:
 * توقيعٌ بمفتاح التطبيق من `CertificateIssuer`، وبصمةٌ **بلا مفتاح** من مسار شهادات
 * التطوّع القديم، وقيمٌ مزروعة من سيدرات العرض.
 *
 * فلو بدأ التحقّق بلا ترحيل لظهرت شهاداتٌ **صحيحة** — صدرت من محرّكنا لأصحابها —
 * موسومةً بـ«التوقيع لا يطابق». وذاك اتّهامٌ باطلٌ في وجه صاحبه أمام جهةٍ تتحقّق،
 * ولا يقلّ ضررًا عن التزوير الذي نحرس منه.
 *
 * فتُختَم الصفوف القائمة كلّها بتوقيعها الصحيح: ما كُتِب قبل وجود المتحقِّق
 * يُصدَّق **مرّةً** لأنّه كُتِب بيد الخادم نفسه. ومن بعد هذه اللحظة لا يُكتَب صفٌّ
 * إلّا من `CertificateIssuer` موقَّعًا، وأيّ صفٍّ يُدَسّ أو تُبدَّل لقطتُه يسقط
 * في «التوقيع لا يطابق» ولا يُعلَن ساريًا أبدًا.
 *
 * وهي بلا `down()` عن قصد: التراجع يعني إعادة توقيعاتٍ لا يعرف أحدٌ صيغتها،
 * والصفوف نفسها لم تُمَسّ إلّا في عمودٍ واحد يُعاد اشتقاقه متى شئنا.
 */
return new class extends Migration
{
    public function up(): void
    {
        $sealed = app(CertificateSignature::class)->backfill();

        if ($sealed > 0) {
            // يُطبَع لا يُخفى: عدد ما خُتِم جزءٌ من الإعلان نفسه
            echo "  ↳ خُتِمت {$sealed} شهادةً قائمةً بتوقيعها الصحيح (خطّ أساس التحقّق).".PHP_EOL;
        }
    }
};
