<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

/**
 * ⭐ **«بدون مدة انتظار» صار انتظارًا 24 ساعة** — تصحيح افتراضيَّي المخطّط (4.2).
 *
 * نصّ 4.2 حرفيًّا: «**يكلّف تذكرة واحدة** تُخصَم **بمجرد الدخول** (سواء جاوب أو
 * ما جاوبش)، و**بدون مدة انتظار** (لا يوجد الـ 20 ثانية هنا). **كل دخول =
 * تذكرة**». فالنصّ لا يعرف سقفًا للمحاولات ولا مدّةَ انتظارٍ بعد الرسوب، والذي
 * يحكم الدخول هو **التذكرة وحدها**.
 *
 * وكان المخطّط يفتتح كلّ امتحانٍ بـ`attempts_allowed = 1` و
 * `retry_cooldown_hours = 24`، فكلّ امتحانٍ يُنشئه الأدمن من فورم التدريب
 * (`CourseFormService::syncExam` لا يمرّر العمودين) يولد مخالفًا للنصّ: من رسب
 * لا يعيد أبدًا، ولو رفع الأدمن السقف بقي ممنوعًا أربعًا وعشرين ساعة.
 *
 * دليل تشغيل قبل هذه الهجرة: على امتحانٍ افتراضيّ، بعد الرسوب ردّ
 * `POST /exams/6/begin` بـ«خلصت محاولاتك في الامتحان ده»، ثمّ برفع السقف وحده
 * ردّ بـ«لسّه بدري على المحاولة الجاية» — وفي الحالتين **بلا خصم تذكرة**.
 *
 * **صفرٌ = بلا حدّ / بلا انتظار** — وهو الافتراضيّ الآن. والمفتاحان يبقيان
 * قابلين لضبط المالك من تاب «التقييم» (2.13 · 12.4-ب): الملغى هو الافتراضيّ
 * المخالف لا إمكانيّة الضبط.
 *
 * ولا فقدَ بيانات (2.11-د): العمودان يبقيان بأنواعهما، ولا تُمسّ إلّا الصفوف
 * الحاملة **عين القيمتين المخالفتين** اللتين لم يخترهما أحد — وهما ما كان
 * المخطّط يكتبه بنفسه على كلّ امتحانٍ جديد.
 */
return new class extends Migration
{
    public function up(): void
    {
        if (! Schema::hasTable('exams')) {
            return;
        }

        Schema::table('exams', function (Blueprint $table) {
            // 0 = بلا حدّ للمحاولات — «كل دخول = تذكرة» (4.2)
            $table->unsignedInteger('attempts_allowed')->default(0)->change();
            // 0 = بلا مدّة انتظار بعد الرسوب — «بدون مدة انتظار» (4.2)
            $table->unsignedInteger('retry_cooldown_hours')->default(0)->change();
        });

        // ما وُلد بالافتراضيّ المخالف يُصحَّح؛ وما اختاره المالك صراحةً يبقى
        DB::table('exams')->where('attempts_allowed', 1)->update(['attempts_allowed' => 0]);
        DB::table('exams')->where('retry_cooldown_hours', 24)->update(['retry_cooldown_hours' => 0]);
    }

    /**
     * الرجوع لا يعيد الانتظار: الافتراضيّ القديم مخالفةٌ لنصّ 4.2 لا حالةٌ
     * يُتراجَع إليها. والعمودان باقيان، فالمالك يضبطهما متى شاء.
     */
    public function down(): void {}
};
