# 🗂️ `resources/views/placement` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** شاشة **بناء الاختبار التمهيديّ** في لوحة الإدارة (2.5-د-2 · 12 · 24 — محتوى الـOnboarding).
- **البنود الحاكمة:** 2.5-د-2 · 12 (صلاحيّات الأدمن) · 12.2.2 (`placement_test.*`) · 24 (محتوى الـOnboarding) · 2.15-ج · 2.15-أ-7 · 2.13.
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **شاشات/أجزاء بليد (2):**
  - `admin/index.blade.php` — جدول أسئلة الاختبار التمهيديّ + بوب-أب [سؤال]
  - `admin/partials/row-actions.blade.php` — إجراءات الصفّ بالصلاحيّة
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
- ✅ **«جدولٌ بلا شاشة» أُغلِق:** `placement_test_questions` كان موجودًا منذ هجرته و`PlacementTest` تقرأ منه وشاشة المتدرّب مبنيّة — **ولا مسارَ ولا فيو ينشئ سؤالًا واحدًا**، فيبقى البنك فارغًا وتُتخطّى خطوة التصفية المنصوصة في 2.5-د صامتةً. الشاشة الآن تنفّذ نصّ 24 حرفيًّا: جدول (السؤال · النوع · مكافأة XP · مكافأة تذاكر · الترتيب سحبًا · الحالة · إجراءات) + بوب-أب [سؤال] (النصّ + النوع + الوسائط + الخيارات + الإجابة الصحيحة + XP + تذاكر) + الحالة الفارغة «لا أسئلة تمهيديّة — أضِف أوّل سؤال» + **تحذير HTML غير صالح قبل الحفظ**.
- ⚠️ **موضع الشاشة:** 24 يضع جدول الأسئلة **تابًا داخل شاشة «محتوى الـOnboarding»** بسايد-نافها (التعليمات · الاختبار التمهيديّ · تحت المراجعة · تم قبول حسابك · OTP والألقاب · الاعتماد). وتلك الشاشة (`resources/views/admin/settings/**`) **بيد إيجنت آخر** في هذه الجلسة، فبُنيت هنا شاشةً مستقلّة بمسار `admin.placement-test.index`. ⇐ ناقص: دمجها كتابٍ داخل «محتوى الـOnboarding» حين تُبنى تلك الشاشة، أو الإبقاء عليها وربطها من سايد-نافها.
- ❌ **بقيّة تبويبات «محتوى الـOnboarding» غير مبنيّة هنا** (كود HTML لصفحتَي «تحت المراجعة» و«تم قبول حسابك» بمعاينة) — إعداداتها موجودة في `2026_08_09_120030` ولا شاشة لها.
- ⚠️ **لا معاينة للسؤال كما سيراه المسجّل** — 24 ينصّ على معاينة لصفحتَي HTML لا للسؤال، فلم تُخترَع.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** الشاشة تعمل من طرفٍ إلى طرف.
- **آخر نقطة وصلنا لها:** 2026-08-03 — `GET /admin/placement-test` ⟵ 200 (كان 404: لا مسار أصلًا) · `POST` أنشأ سؤالين · `POST …/reorder` بدّل الترتيب (2،1) · HTML غير متّزن ⟵ **422** بنصّ التحذير، والمتّزن ⟵ 302 · `GET …/export` ⟵ CSV بترويسة عربيّة · وشاشة المتدرّب `GET /onboarding/placement` صارت تعرض السؤالين فعلًا (`answers[1]` ثلاثة خيارات و`answers[2]` نصّ) بعد أن كانت تُظهر «مافيش أسئلة». و375px بلا تمرير أفقيّ (sw=cw=375).
- **الخطوة الجاية:** ربطها بسايد-ناف «محتوى الـOnboarding» حين تُبنى (24).
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `app/Http/Controllers/Admin/PlacementTestAdminController.php` · `app/Services/Onboarding/PlacementTest.php` · `routes/parts/admin-content.php` · هجرة `2026_09_01_100010`.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `resources/views/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-03 — بيدٍ (المجلّد جديد).
- **آخر لمسة للمجلّد:** 2026-08-03 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
