# 🗂️ `resources/views/admin/studio` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** «studio» ضمن: شاشات لوحة الإدارة باثنَي عشر قسمًا في السايد بار (12.0).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.13-أ · 2.13 · 2.14-ب · 12.5-ب · 12.14-أ · 12.14-د · 12.14-ج · 24.2
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **شاشات/أجزاء بليد (2):**
  - `edit.blade.php` — المقاس الجاهز عمودٌ في القاعدة (`preset`) — فيُرسَل باسمه لا كمساعدٍ بصريّ
  - `index.blade.php` — «مجلّدات ووسوم · بحث» (12.14-أ) — العمودان بلا فلترٍ كانا زينةً في القاعدة
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**أوديت 12.14 — 2026-08-03، وإغلاق الفجوات — 2026-08-04:**

- ✅ **رفع الفريم/الخلفيّة اتقفل (2026-08-04):** حقل `frame_path` في المحرّر **يفتح نفس بوب-أب مكتبة الوسائط** (`admin.courses.partials.media-picker-modal` + `data-media-pick="frame_path"`) — مصدرٌ واحد لا نسختان (2.14-ب). ومسارٌ لا وجود له على القرص **يُرفَض** فلا يظنّ المصمّم أنّه رفع. **ومُثبَت بالتشغيل أنّ المحرّك يرسمه:** بكسل (5,5) في المعاينة انتقل من `#030d17` إلى `#dc1e1e` وبصمة الصورة تغيّرت (`165f846ab8c1a5ee` ⟵ `8b14b26330262e3d`).
- ✅ **المجلّدات والوسوم اتقفلت (2026-08-04):** حقلان مفصولان بفاصلة بمنظّف مكتبة الوسائط نفسه (`MediaLibrary::cleanTags`)، **وفلترا مجلّد ووسم** في القائمة + رقائق قابلة للضغط على كلّ كارت.
- ✅ **وثلاثة أعمدة أخرى كانت بلا حقل (2026-08-04):** `preset` (كان `<select>` **بلا `name`** فلا يصل الخادمَ أبدًا) · `purpose` (مُصادَق عليه بلا حقل فيبقى `marketing` أبدًا) · `language` (بلا كاتب ولا قارئ).
- ⬜ **عمودٌ باقٍ بلا حقل — بقصد:** `name_particles.is_active` يقرؤه `ShortName` (`where('is_active', true)`) ولا يكتبه أحد، فالأداة تُحذَف ولا تُعطَّل. **و12.14-ج ينصّ على «إضافة/حذف» فقط** — فلم نخترع مفتاحًا ليس في النصّ. لو أُضيف يومًا فليكن بنصٍّ حاكم لا باجتهاد.
- ✅ **[أُغلِقت 2026-09-10] «نفس محرّك 12.5-ب بكلّ إمكاناته» (12.14 · إشارة 12.5-ب،
  سطر 2597-2600):** كانت الفجوة القديمة «محرّكان منفصلان: مصمّم الشهادات فيه Snap
  وشبكة محاذاة ومعاينة بالمقاس الحقيقيّ، ومحرّر الاستوديو بلا سحب وإفلات وبلا
  Snap» — أُغلِقت فعلًا بكوميت `4f9673c` («feat(studio): give Image Studio a
  real drag-drop canvas»): كانفس `#studio-canvas` حقيقيّ بـpointerdown/move/up،
  شبكة محاذاة قابلة للتفعيل (`data-studio-snap`)، ومعاينة بالمقاس الحقيقيّ
  (نفس `ImageRenderer::draw()` الذي يُنتِج الصورة الفعليّة). الفرق الوحيد
  المتعمَّد: إحداثيّات الاستوديو **بكسل مطلق** لا كسرٌ 0-1 كالشهادات — لازمة
  لإعادة التوزيع النسبيّ عند تغيير المقاس (`TemplateLayers::rescale`، 12.14-أ).
  **والتحقّق الآن مُثبَتٌ باختبار Feature حقيقيّ** لا بمعاينة يدويّة فقط:
  `AdminSystemImageStudioTest::test_dragging_a_layer_saves_its_new_pixel_position`
  يرسل X/Y جديدَين كما يرسلهما الكانفس فعلًا (نفس حقول `layers[i][key]`
  المخفيّة) ويتأكّد أنّهما يصلان `ImageTemplate::layers` **وأنّ الرسم على
  الخادم يتغيّر فعلًا** (بصمة PNG مختلفة قبل/بعد لا افتراضًا)، و
  `test_text_layer_size_color_and_alignment_are_saved` يثبت نفس المسار
  للحجم واللون والمحاذاة والدوران. ⚠️ **Snap تقريبٌ في الواجهة (JS) فقط** —
  `pointermove` في `edit.blade.php` يقرّب `fx/fy` للشبكة قبل كتابتها في
  الطبقة، و`TemplateLayers::sanitize()` لا يعرف عن Snap أو `grid_step` شيئًا؛
  `test_server_stores_off_grid_pixel_positions_exactly_snap_is_client_side_only`
  يثبت أنّ موضعًا خارج أيّ خطّ شبكة يُقبَل ويُخزَّن بالحرف على الخادم — فلا
  ادّعاء بتغطية تقريبٍ خادميّ لا وجود له.
- ✅ ب-2 مقفولة: الشاشة صارت تمتدّ من `layouts.admin` فتُعرَض داخل سايد بار الإدارة — انظر `resources/views/admin/_STATUS.md`.
- **مُثبَت شغّالًا:** لوحة الطبقات (ظاهرة/مقفولة/▲/▼/✕) بـX/Y والدوران والحجم والشكل (دائرة/مربّع/دائرة بحدّ) ونمط القصّ وحدّ الأحرف وسلوك التجاوز (تصغير/قصّ) · القائمة المقفولة للحقول (والمحافظة فيها، والموبايل والبريد غير موجودَين أصلًا) · معاينة ببيانات مستخدم حقيقيّ · **التوليد الجماعيّ ZIP يعمل فعلًا**: `POST /admin/studio/1/batch` أنتج أرشيفًا فيه `UDASH001.png` و`UDEMO007.png` و`UDEMO008.png` مرسومةً على الخادم بخطّ Cairo، وطبّقت **قاعدة الاسم المختصر** («سلمى عبد الرحمن محمود» ⟵ «سلمى عبد الرحمن») وحملت **تاريخ اللقطة وشعار المنصّة** · قائمة أدوات الاسم قابلة للتعديل من الشاشة · زرّ «استخراج كصورة» الموقَّع يرجع PNG 1080×1080 حقيقيًّا.
- ⚠️ عطبٌ بصريّ في الصورة المولَّدة: الأقواس تنعكس في RTL («مبيعات )كوينز(»).
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** أعمدة الاستوديو بلا حقول **اتقفلت كلّها** — بلا فجوةٍ واحدة باقية في مطابقة العمود ⟵ الحقل.
- **آخر نقطة وصلنا لها:** طلب HTTP حقيقيّ على قاعدةٍ خاصّة في السكراتش: رُفِع فريم من البوب-أب وحُفِظ وقُرِئ عند إعادة الفتح، وثبت رسمُه بالبكسل والبصمة؛ ولقطات على 1280 و375 بـ`scrollWidth === innerWidth` في الحالتين.
- **الخطوة الجاية:** لا شيء عالق من نصّ 12.14-أ نفسه بعد إغلاق فجوة السحب-والإفلات
  أعلاه. تطبيقا 12.5-ب و12.14 يظلّان كودَين متوازيَين متكافئَي الإمكانات
  (سُطِّر عمدًا في `4f9673c` لتصفير مخاطرة الانحدار على توقيع الشهادات) —
  توحيد الجافاسكربت نفسه حرفيًّا (لا الإمكانات فقط) يبقى تحسينًا معماريًّا
  اختياريًّا لا فجوةً في النصّ الدستوريّ.

- **دفعة النصّ المحروق (2026-08-04):** كلّ نصٍّ عربيّ ظاهر في قوالب هذا المجلّد صار `setting('مفتاح', 'النصّ الحاليّ')` — **91 موضعًا محروقًا ⟵ صفر**، و**86 مفتاحًا** معرَّفةً في `database/seeders/AdminScreenText*DemoSeeder.php` (يستدعيها `SettingDefinitionsSeeder` في مسار الإنتاج). ولا حرفَ تغيّر: الافتراضيّ هو النصّ الذي كان. والعتبة أُحكِمت على هذا المجلّد في `docs/hardcoded-text-baseline.json`.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `resources/views/admin/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-09-10 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-09-10 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
