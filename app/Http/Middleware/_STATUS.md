# 🗂️ `app/Http/Middleware` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** حرّاس الطلب: باب لوحة الإدارة · حارس الصلاحيّة على كلّ مسار · سياق العضويّة النشطة · جدار الاحتواء · قفل الصيانة · جدار الإقرار الإلزاميّ · حارس عضويّة القسم.
- **البنود الحاكمة:** 12.2.1-أ · 12.2.1-ب · 12.1-متقدّم-1 · 12.7-و-1 · 13.2 · 24.4-7 · 2.15-أ-7 · 23-0.2.
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **حرّاس (7):**
  - `EnsureAdminPanel.php` — `admin.panel`: الباب لمن له صلاحيّة إداريّة.
  - `EnsurePermission.php` — `permission:key[,key]`: الفاصلة = «أيٌّ من»، ومع تفضيل المفتاح الإداريّ داخل `admin.*`.
  - `SetMembershipContext.php` — تثبيت العضويّة النشطة ومشاركتها مع الفيوز.
  - `EnsureAccountNotContained.php` — المحظور/المعلَّق لا يفتح صفحة.
  - `EnsureNotUnderMaintenance.php` — قفل الصيانة العامّ.
  - `EnsureCriticalAcknowledged.php` — «قبل المتابعة»: لا تصفّح قبل إقرار التوجيه الحرج (13.2).
  - `EnsureDepartmentMembership.php` — `department.member`: شاشة «قسمي» بالعضويّة لا بمشي النطاق (24.4-7).
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**أوديت المرحلة 5 — ما أُثبِت بالتشغيل:**

- ✅ **تغطية الحرّاس:** من **372 مسار `admin.*`** لا يوجد إلّا **مسارٌ واحد** بلا
  `admin.panel` وهو `admin.impersonate.stop` (عن قصد: زرّ الخروج من الانتحال)،
  و**مسارٌ واحد** بلا `permission:` وهو `admin.dashboard` (الباب نفسه). لا ثغرة تغطية.
- ✅ **`SetMembershipContext`:** التقييم يقع داخل العضويّة النشطة — النطاقات الستّة
  أعطت سلّمًا صحيحًا مقيسًا من العضويّة، ولا سلطة عابرة للكيانات.

**✅ أُصلِح (جولة أ-3 · ب-5) — بدليل تشغيل ومعه اختبار طفرة:**

1. ✅ **`EnsurePermission` صار يوصل الهدف إلى التقييم (12.2.1-ب):** الحارس يلتقط
   **آخر نموذجٍ مربوطٍ** في بارامترات المسار ويمرّره إلى `allows()`. والهدف يُمرَّر
   حين **يُقاس عليه النطاق** فقط: `User` · `Membership` · `Entity`، أو سجلٌّ يحمل
   `user_id` أو `entity_id`. أمّا `owner_id` وحده فهو في هذا المستودع **مُنشِئ**
   السجلّ لا موضوعه (حرب تركيز · اجتماع)، وقياسه بـSELF يقلب المعنى.
   وشرطٌ ثانٍ: أن يكون البارامتر **موضوع المسار** — أي مسمًّى باسم نموذجه
   (`{user}` · `{membership}` · `{task}`)؛ فالمربوط باسمٍ آخر (`{opponent}` ·
   `{party}`) طرفٌ **داخل الإجراء** لا سجلٌّ يُطَّلع عليه، وقياس النطاق عليه يقلب
   المعنى (يمنع صاحب `wars_matches.create@SELF` أن يتحدّى أحدًا).
   **مقيس:** `GET /volunteer/org/node/{membership}` بـ`org_chart.view@SELF` على
   عضويّةٍ أجنبيّة ⟵ **403** (كانت 200 ببياناتها)، وعلى عضويّته هو ⟵ **200**؛
   وبـ`@TRACK` على عضويّةٍ في قسمٍ آخر من نفس المسار ⟵ **200** (كانت 403).
   **والطفرة تقتل:** إعادة النداء بلا هدف ⟵ سقط اختباران فورًا.
2. ✅ **`EnsureAdminPanel` فُتِح لأصحاب الصلاحيّة بلا انقلابٍ مضادّ (12.2.1-أ):**
   الباب لم يعد يُحسَب بقائمة مفاتيحَ تحرس `admin.*` (كانت 246 من 1033)، بل
   بـ**مصدر الصفّ**: صلاحيّةٌ من دورٍ **خارج طبقة `user`** أو من استثناءٍ فرديّ.
   **مقيس:** الخمسة (`store_products.list` · `events.list` · `referrals.view` ·
   `courses.manage` · `paths.manage`) بنطاق ALL ⟵ **200 على `/admin`**، ودور
   **«فريق التوظيف»** ⟵ **200**؛ والمتدرّب — وهو يحمل `store_products.list@ALL`
   **نفسها** من قالبه — ⟵ **403**، و«تحت المراجعة» ⟵ **403**، والمسح الكامل
   لكلّ مسارات `admin/` عليهما ⟵ **لا تسريب**.

**✅ أُصلِح (جولة ب-6 · 24.4-7) — بدليل تشغيل ومعه اختبار طفرة:**

3. ✅ **`EnsureCriticalAcknowledged` — «قبل المتابعة» صار شرطَ مرور (13.2):**
   كان الإقرار الإلزاميّ مبنيًّا بالكامل ولا يُفرَض في سطرٍ واحد. الحارس ينادي
   `AcknowledgementGate` (في `app/Services/Notifications`)، وموضعه في `web`
   **بعد** جدار الاحتواء وقفل الصيانة فلا تتنازع الجدران على الردّ.
   **مقيس:** `/dashboard` و`/library` و`/events` و`/store` و`/profile` لصاحب
   توجيهٍ حرجٍ غير مُقَرّ ⟵ **302 إلى `/announcements`** (كانت 200)، والطلب غير
   المتزامن ⟵ **403 بعلَم `acknowledge_required`**؛ وبعد الإقرار ⟵ 200 فورًا.
   **ولا حبس:** الخروج والإقرار نفسه والأمان ودليل المستخدم مفتوحةٌ بقائمة
   إعدادات — والطفرة تقتل: إلغاؤها ⟵ **9 سقطات** منها حلقةُ تحويلٍ على
   `/announcements` نفسها.
4. ✅ **`EnsureDepartmentMembership` — «قسمي» تُحرَس بالعضويّة (24.4-7):** بعد
   أ-3 صار `org_chart.view@SELF` — سقف الكوردنيتور (12.2.3-ب-16) — يعني «نفسه»
   حرفيًّا، فانقفلت شاشةٌ نصُّها «تاب لكلّ **عضوٍ في القسم**» في وجه أصحابها:
   الكوردنيتور لم يعد يفتح بوب-أب زميله ولا يطلب رقمه (13.4-م-2). والحارس
   يقبل **المفتاح + (النطاق أو عضويّة القسم)** — فلا يُرخي الصلاحيّة ولا يحلّ
   محلّ المحرّك: `allowsOnRecord` تُسأل أوّلًا كما هي.
   **مقيس:** كوردنيتور `@SELF` ⟵ **200** على عضويّة زميله في قسمه (وعلى فرعيٍّ
   آخر من نفس القسم)، وكوردنيتورٌ **من قسمٍ آخر** بنفس المفتاح ⟵ **403**،
   وعضوٌ بلا المفتاح أصلًا ⟵ **403**. **و«الهيكل التنظيميّ» لم يُمَسّ:** نفس
   الكوردنيتور ⟵ **403** على `volunteer.org.node` لعضويّة زميله.
   **والطفرة تقتل:** إسقاط فرع العضويّة ⟵ 4 سقطات · إسقاط شرط المفتاح ⟵ سقطة ·
   إسقاط حصر القسم ⟵ سقطة.

⬜ **المتبقّي:**
- **`EnsureDepartmentMembership` استثناءٌ مقصود لا نمطٌ عامّ:** لا يُنسَخ إلى
  شاشةٍ أخرى إلّا إذا كان نصُّها يبني الحقّ على **العضويّة** لا على سلسلة
  الإشراف — وإلّا صار بابًا يلتفّ على 12.2.1-ب.
- **`AcknowledgementGate` يبني الفيد** حين يوجد منشورٌ حرجٌ حيّ (الفحص الرخيص
  يسبقه) — يحتاج كاشًا لعمر الطلب مع اتّساع القاعدة.
- **الحارس يمرّر السجلّ إلى النطاق وحده** (`AccessEngine::allowsOnRecord`) ولا يفعّل
  به شروط المصفوفة: `user_sessions.delete` شرطُها «ليس نفسه» ومعناها في الشاشة
  «ليست الجلسة الحاليّة»، فقياسها على الجهاز — وهو جهاز صاحبه — كان يقفل عليه إنهاء
  جلساته هو. ومراجعة نصوص الشروط لتصير قابلةً للقياس على السجلّ بندٌ في
  `app/Support/Access/_STATUS.md`.
- ✅ **حُسِم (كان أثرًا وظيفيًّا معلَّقًا):** «الكوردنيتور لم يعد يفتح بوب-أب
  عضويّة زميله» — الحسم أنّ **«قسمي» تُحرَس بالعضويّة** (`department.member`)
  لأنّ 24.4-7 يبني الحقّ فيها على عضويّة القسم، بينما **«الهيكل التنظيميّ»**
  يبقى على المحرّك بالنطاق. انظر البند 4 أعلاه.
- **الهدف يصل حيث يُقاس فقط:** المسارات ذات البارامترات القياسيّة (`{code}` ·
  `{token}`) أو النماذج التي لا تحمل صاحبًا ولا كيانًا (تدريب · درس · فعاليّة ·
  امتحان) تبقى على حارسها في المجال — وهي نفس قاعدة `guard => 'domain'` المعتمَدة
  في `config/access.php`. وتحويلها إلى نطاقٍ مقيس يحتاج عمود صاحبٍ أو كيان عليها.
- **`ScopeFilter` يبقى مسؤولًا عن بيانات القوائم** — الحارس يحكم على السجلّ الواحد.
- **⚠️ لا `_STATUS.md` مولَّد لهذا المجلّد:** خارج جذور `php artisan docs:status`
  (`app/Services` · `app/Http/Controllers` · `resources/views`) — فهذه الوثيقة مكتوبة يدًا.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** أربع فجوات مُصلَحة ومختبَرة (أ-3 باب النطاق · ب-5 باب اللوحة ·
  ب-6 فرض الإقرار · 24.4-7 حارس «قسمي»).
- **آخر نقطة وصلنا لها:** 2026-08-03 — `tests/Feature/Access/ScopeOnRequestTest.php`
  و`AdminPanelGateTest.php` و`tests/Feature/Announcements/AcknowledgeEnforcementTest.php`
  و`tests/Feature/Volunteer/Org/DepartmentMembershipGuardTest.php`، وكلّ حارسٍ
  منها قُتِلت طفرتُه.
- **الخطوة الجاية:** مراجعة بقيّة الكناترول التي تعيد تطبيق النطاق بيدها بدل المحرّك.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **المرجع الحاكم:** `دستور اساسي.md` (12.2.1) · **وكيف نكتب:** `docs/BUILD.md`.
- **المحرّك:** `app/Support/Access` · **الإعداد:** `config/access.php` · **التسجيل:** `bootstrap/app.php`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** — (خارج نطاق `php artisan docs:status`).
- **آخر لمسة للمجلّد:** 2026-08-03 — Claude (ب-6 فرض الإقرار · 24.4-7 حارس «قسمي»).
<!-- تلقائيّ:نهاية:التحديث -->
