# 🗂️ `app/Http/Controllers/Admin` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** متحكّمات لوحة الإدارة (12).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.5-د · 2.6-ب · 2.7 · 2.9 · 2.11-ط · 2.11-ب · 2.13-د · 2.13-و · 2.13-أ · 2.13-هـ · 2.13 · 2.14-ب
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (48):**
  - `AdsController.php` — الإعلان المدفوع: إعادة الاستهداف والجمهور المشابه (21.3).
  - `ApiKeyController.php` — إنشاء/تدوير/إبطال مفاتيح API (12.15-أ) — إجراءات تاب «API» في شاشة المطوّرين.
  - `ArticleAdminController.php` — مركز المقالات (21.2-أ) — نظام تحريريّ كامل بدورة نشر إلزاميّة.
  - `AvailabilityAdminController.php` — إدارة الإتاحة الزمنيّة للتدريبات (الدستور 5 · 12.4).
  - `CertificateAdminController.php` — إدارة الشهادات (12.5 · 24.1) — أربعة تبويبات بترتيب منطقيّ:
  - `CountriesController.php` — 12.7-د «بيانات الدول»: عرض/تحديث مصدر `dr5hn` مع **فحص فروق النسخة قبل
  - `CourseAdminController.php` — التدريبات (12.4-ب · 24.1): جدول بفلاتر + فورم متعدّد التابات.
  - `CvTemplateAdminController.php` — شاشة إدارة قوالب الـCV (الدستور 9 · 12.0 ⟵ «قوالب الـCV»).
  - `DashboardController.php` — لوحة القيادة (الدستور 12.3 · 24.1).
  - `DelegationAdminController.php` — ⭐ شاشة إدارة الغيابات والتفويض المؤقّت (الدستور 23-6 · 24).
  - `DevelopersController.php` — 🧩 المطوّرين — API وWebhooks والطرفيّة (12.15 · v5.5/v5.6، سجلّ القرارات 25).
  - `EventAdminController.php` — الفعاليّات (12.11 · 13.3 · 24.3).
  - … و36 غيرها.
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**أوديت المرحلة 6 — 2026-08-03 (ما يخصّ الكنترولرات):**

- ✅ **`MediaController@picker` صار له مستدعٍ:** والعلّة كانت أعمق من وصلة — الفيو بُني **صفحةً كاملة** بنسخٍ للحافظة، و12.4-د/24.1 يطلبان **بوب-أب**. صار للفيو وجهان (صفحة · جزءٌ عارٍ بـ`?fragment=1`) ووُصِل من `admin/courses/form.blade.php` و`paths.blade.php` عبر `partials/media-picker-modal.blade.php`.
- ✅ **و`$multiple` صار له قارئ (2026-08-04):** القالب يقرؤه فيرسم علامات الاختيار ويمرّره مع البحث والترقيم، والبوب-أب يجمع المحدَّد؛ ومستهلكه الأوّل **مرفقات الدرس**. ومعه رُفِعت `mediaItems` من `LessonAdminController::show()` — كانت قائمةً مقصوصةً بـ`media.picker.limit` (12) تُطبَع Checkboxes بلا بحث فيتعذّر إرفاق الملفّ الثالث عشر. وأُضيف علَم `attachments_managed` ليفرّق بين «لم يُرسَل» و«فُرِّغ». الحارس: `tests/Feature/Admin/Content/LessonAttachmentsPickerTest.php`.
- ✅ **`StoreAdminController@updateBundle` صار يكتب `og_image_path` (2026-08-04 · 21.1-أ):** العمود كان يُقرَأ في `<head>` بلا حقلٍ يكتبه — «عمودٌ ومصادقةٌ بلا حقل». صار له حقلٌ في تاب [الهويّة] يفتح **نفس بوب-أب المكتبة** (مصدرٌ واحد لا منتقٍ ثانٍ)، والفارغ يُخزَّن `null` فيبقى الارتداد للغلاف حيًّا. الحارس: `tests/Feature/Store/BundleOgImageTest.php`.
- ✅ **`LessonAdminController@duplicateSection` (2026-08-04 · 12.4-هـ):** الثالث المنصوص («تكرار/نسخ (Duplicate) لتدريب · سيكشن · درس») صار مبنيًّا بصلاحيّة **`sections.create`** من 12.2.2 — بلا مفتاحٍ مخترَع. الحارس: `tests/Feature/Admin/Content/SectionDuplicateTest.php`.
- ✅ **`CourseAdminController@duplicate` صار له زرّ** في «⋯» وفي كارت الموبايل بصلاحيّة `courses.create` (12.4-هـ) — مُثبَت: 15 ⟵ 16 تدريبًا ونسخةٌ `draft`.
- ❌ **`StatsController@export` يتجاهل `format`:** `xlsx` و`pdf` يخرجان CSV بنفس الترويسة والحجم (12.8).
- ✅ **صار كنترولر لأسئلة الاختبار التمهيديّ:** `PlacementTestAdminController` (فهرس · إنشاء · تعديل · تبديل الحالة · حذف · ترتيب · تصدير) بموارد `placement_test.*` من 12.2.2، وشاشته `resources/views/placement/admin` — وفيه **فحص اتّزان وسوم HTML قبل الحفظ** كما ينصّ 24. ⚠️ وموضعها المنصوص تابٌ داخل «محتوى الـOnboarding» (شاشة إيجنت آخر) فبُنيت مستقلّةً مؤقّتًا — التفصيل في `resources/views/placement/_STATUS.md`.
- ⚠️ **61 من 77 فيو إدارة تمتدّ `layouts.app`** فتُعرَض داخل سايد بار المتدرّب — والكنترولرات هي التي ترجعها؛ التفصيل في `resources/views/admin/_STATUS.md`.
- **مُثبَت شغّالًا:** كلّ مسارات `/admin` المفتوحة ردّت 200 للمالك و403 لمن لا يملك (`support_admin` ⟵ 403 على الإعدادات والمكافآت والاستوديو والشهادات والتدريبات والماليّات) · `UserModerationController` له أثرٌ حقيقيّ: التعليق أنهى جلسة المتدرّب فورًا ومنع دخوله، والحظر منعه كذلك · `CountriesController` يفصل الفحص عن الدمج ويحمي المرتبط بمستخدم · `MaintenanceController` + `EnsureNotUnderMaintenance` يقفلان المنصّة فعلًا (503).
- ⚠️ رسالة رفض الدخول لحسابٍ معلَّق/محظور هي رسالة «البيانات مش مظبوطة. راجع الكود أو البريد وكلمة السرّ» — أي يُقال لصاحب الحساب المعلَّق إنّ بياناته خاطئة.

**✅ أُصلِح — سقف نطاق المصفوفة وقت الكتابة (أ-7 · 12.2.2 · 12.2.1-د):**

- ✅ **`PermissionController@update`:** التصديق كان `['required','string']` على النطاق،
  فيُكتَب `scope=ALL` على مفتاحٍ سقفُه TRACK **وينفُذ**. الآن: النطاق من القائمة
  الستّة نصًّا (`Rule::in`)، ثمّ **رفضٌ صريح** إن تجاوز `allowed_scopes` — برسالةٍ
  من الإعدادات تسمّي النطاقات المتاحة. ولا يُكتَب صفٌّ ولا يُصغَّر نطاقٌ في السرّ.
  والمنع لا يخضع للسقف: توسيعه تشديدٌ لا تصعيد (12.2.1-ز-1).
- ✅ **`RoleController@update`:** كان محرّر الأدوار يقع في العكس — `@ALL` **يُحفَظ
  `@SELF`** بلا إخبار (استبدالٌ بالنطاق الافتراضيّ داخل `RoleEditor::save`)، وهو
  نقيض «يرى بعينه ما مُنِح». الآن يُفحَص كلّ صفٍّ **قبل** الحفظ، وأيّ نطاقٍ فوق
  السقف يردّ الحفظ كلّه برسالةٍ لكلّ سطر — **ولا سطر يُكتَب**.
  **مقيس:** `org_chart.view@ALL` ⟵ لا صفّ + رسالة · و`@TRACK` ⟵ يُحفَظ TRACK كما اختِير.
- ⚠️ **باقٍ:** التضييق الصامت نفسه ما زال داخل `RoleEditor::save()` (خارج نطاق هذه
  الجولة) — صار غير قابل للوصول من هذا الكنترولر، لكنّه يعمل لمن ينادي الخدمة مباشرةً.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** أوديت فقط — بلا إصلاح (قواعد الأوديت 6).
- **آخر نقطة وصلنا لها:** فُتِح كلّ مسار GET تحت `/admin` بطلبٍ حقيقيّ (110 مسارًا) ونُفِّذت عمليّات POST حقيقيّة على المكافآت والشهادات والتعليمات والدول والصيانة والحظر.
- **الخطوة الجاية:** صيغتا التصدير في `StatsController@export` · دمج شاشة الاختبار التمهيديّ في «محتوى الـOnboarding» · رسالة رفض الدخول لحسابٍ معلَّق.
- **⚡ `SettingsAdminController` (2026-08-04):** `index()` صارت تُصيّر **رؤوس كروت المجموعات** (عدّةُ SQL) ودفعةً واحدة للمجموعة المفتوحة بدل `forTab()`+`groupedForTab()` (تحميلان كاملان للجدول). وأُضيفت `groupBatch()` — نقطة `GET /admin/settings/batch` تردّ قطعة HTML لدفعةٍ من مجموعة، بحارسَي التاب والمجموعة ورسالتَي خطأٍ تقولان ماذا يفعل (2.17-ب). القياس: `/admin/settings` **23,637ms/2.16 م.ب/940 حقلًا ⟵ 70ms/150 ك.ب/25 حقلًا**.
- **✅ `WebhookController` جديد (2026-08-06 · 12.15-ب):** تسجيل/إيقاف/استئناف/تدوير سرّ/حذف/إعادة إرسال/اختبار — يُغلق الشقّ الثاني من §12.15 (تاب Webhooks) بجوار `ApiKeyController` من العميل السابق. الحارس: `tests/Feature/Admin/Developers/WebhookManagementTest.php` (20 اختبارًا).
- **✅ `TerminalController` جديد (2026-08-06 · 12.15-هـ · v5.6، سجلّ القرارات 25):** إجراءٌ واحد (`run`) بحارسٍ owner-only صريح (`abort_unless($request->user()?->isPlatformOwner(), 403)`) **بلا** `permission:` على المسار — لا صلاحيّة لهذا التاب إطلاقًا بأمر المالك المباشر. و`DevelopersController::index()` عدَّل حراسته: تاب `terminal` يفحص `isPlatformOwner()` مباشرةً بدل `TAB_PERMISSIONS`. الحارس: `tests/Feature/Admin/Developers/TerminalTest.php` (8 اختبارات، منها Mutation حقيقيّ لحارس الملكيّة وحارس المهلة الزمنيّة).
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `app/Services/Admin` · `resources/views/admin` · `routes/parts` · `tests/Feature/Admin`
- **الاختبارات:** 1 ملفّ Feature في `tests/Feature/Admin` — شغّلها بـ`php artisan test tests/Feature/Admin`.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `app/Http/Controllers/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-06 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-06 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
