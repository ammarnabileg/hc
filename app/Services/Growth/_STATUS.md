# 🗂️ `app/Services/Growth` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** النموّ والسيو والمعاينة المجّانيّة وحلقات الدعوة (21.1 · 21.2).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.5-د · 2.9 · 2.13 · 2.16-ج · 2.16-ب · 2.17 · 7.3 · 12.14 · 21.1-هـ · 21.1-ب · 21.1-ج · 21.1-أ
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (10):**
  - `AcquisitionFunnel.php` — ⭐ **لوحة مصادر الاكتساب** (21.2-ح) بنصّها:
  - `AcquisitionSource.php` — ⭐ **سلسلة الاكتساب المتّصلة** (21.2-ح):
  - `ContentKit.php` — ⭐ الكارت الأسبوعيّ (21.2-د) و**حزمة محتوى المتطوّعين** (21.2-هـ).
  - `InviteLeaderboard.php` — ⭐ لوحة متصدّري الدعوات **شهريًّا** (21.1-ج).
  - `LessonPreview.php` — ⭐ «أوّل درس مجّانيّ كمعاينة» — منفَّذًا لا مكتوبًا (21.1-أ).
  - `OgCardRenderer.php` — ⭐ صورة OG تلقائيّة **لكلّ نوع رابط** (21.1-أ · 12.14): تدريب · مسار · بروفايل ·
  - `ProfileCompletion.php` — بار «أكمل ملفك» ومكافأته (21.1-ب).
  - `SafeHtml.php` — تنقية محتوى المقال قبل عرضه على **صفحة عامّة** (21.2-أ).
  - `Sitemap.php` — ⭐ `sitemap.xml` تلقائيّ (21.2-ب) — «يتحدّث مع كلّ شهادة/تدريب/مقال جديد».
  - `UtmBuilder.php` — ⭐ UTM موحّد على **كلّ رابط تولّده المنصّة** (21.2-ح): دعوات · مشاركات · صور · مقالات.
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
> جرد أوديت المرحلة 10 (21 · 21.1–21.3 · 22) — 2026-08-03. كلّ بند هنا **مثبَت بالتشغيل**.

- ❌ **`Sitemap::robots()` كودٌ ميّت — لا يُنفَّذ أبدًا.** الملفّ الثابت `public/robots.txt`
  يسبق المسار فيخدمه الخادم مباشرةً. الدليل: `curl -I /robots.txt` يرجع بلا
  `X-Powered-By` وبلا `Cache-Control` وبـ`charset=UTF-8` (خدمة ملفّ)، بينما
  `/sitemap.xml` يرجع بترويسات المتحكّم. الأثر: إعداد `growth.robots.disallow`
  **بلا أثر** (خرق 2.13)، وسطر `Sitemap:` يخرج **مسارًا نسبيًّا** `/sitemap.xml`
  لا رابطًا مطلقًا كما يوجب معيار robots.txt — فالزاحف لا يجد الخريطة.
- [x] ~~**`UtmBuilder` يسم الروابط ثمّ يضيع الوسم عند التسجيل**~~ ⇐ **مُغلَقة (د-8، 2026-08-03).**
  الوسم لم يعد يُقرأ من الـquery الجاري: `AcquisitionSource::capture()` يحفظه عند
  **الزيارة**، و`AuthController::register()` يُثبّته على الحساب — والفورم يبقى
  بلا query وبلا حقل `utm_*` واحد (حقلٌ مخفيّ كان سيصير بابًا للتزوير وللتخطّي).
  وسطر `referrals` صار يحمل نفس المصدر بدل `$request->query()` الفارغة.
- [x] ~~**لا حقل اكتسابٍ على المستخدم نفسه**~~ ⇐ **مُغلَقة (د-8).** هجرة
  `2026_08_26_100010_acquisition_source_on_the_user` أضافت `acquisition_utm_source`
  و`_medium` و`_campaign` و`_content` — **الأربعة التي يولّدها `UtmBuilder` نفسه**
  ويخزّنها `tracking_events`، بلا حقلٍ مخترَع لا يذكره 21.2-ح.
  وأصبحت `AcquisitionFunnel` تقرأ السلسلة منها لا من أحداث البكسل.
  **الدليل المقيس بعد الإصلاح** (رحلة HTTP كاملة على `127.0.0.1:8213`):
  `whatsapp_group | زيارات 0 | تسجيل 1 | تفعيل 1 | شراء 1 | التحويل 100%`.
- ⚠️ **`growth.invite_board.*` بلا مفتاح `enabled`** بينما 21.1-هـ يوجب
  «تفعيل/إيقاف كلّ حلقة على حدة». باقي الحلقات لها مفاتيحها
  (`growth.profile_completion.enabled` · `growth.preview.enabled` · `growth.utm.enabled`).
- ⚠️ **`SafeHtml::clean()` يترك نصّ الوسم المحذوف.** `strip_tags` أزال
  `<script>` لكنّه أبقى محتواه نصًّا ظاهرًا: الصفحة العامّة عرضت `alert(1)`
  حرفيًّا داخل `<div class="article-body">`. لا تنفيذ ولا ثغرة، لكنّه تشويهٌ
  ظاهر لمن يقرأ صفحةً عامّة.

- [x] ~~**موافقة الزائر لا تُنقَل إلى حسابه عند التسجيل**~~ ⇐ **مُغلَقة (2026-08-04).**
  21.3-د يعرض البانر «عند **أوّل زيارة**» أي على **الزائر**، فقراره لا يعيش إلّا في
  الكوكي؛ ثمّ كان الحساب يُنشَأ بـ`tracking_consent = NULL` فيضيع القرار على أوّل
  جهازٍ آخر — والرفض الذي لا يسافر ليس «فعليًّا لا شكليًّا»، وكذلك «حقّ السحب في
  أيّ وقت». والعلاج: `Consent::adopt()` (ميثودٌ **واحد جديد** في
  `app/Services/Ads/Consent.php` — بأقلّ لمسة، والمجال مزدحم) يناديه
  `AuthController::createAccount()` بعد `AcquisitionSource::attach()` مباشرةً.
  **⛔ ولا عكسَ للاتّجاه:** بلا كوكي **لا يُكتَب شيء** فيبقى العمود `NULL` وتقرؤه
  `allows()` رفضًا (الصمت ليس موافقة — 2.9)، و**اختيارٌ محفوظ على الحساب لا يُدهَس**
  بكوكي متصفّحٍ عابر (وإلّا أُبطِل سحبٌ وقع).
  **الدليل (أربع رحلات HTTP كاملة على `127.0.0.1:8214`: بانر ⟵ OTP ⟵ شاشتا التسجيل
  ⟵ قراءة من القاعدة):** رفض ⟵ `rejected` · قبول ⟵ `accepted` · تخصيص ⟵ `custom`
  بـ`["analytics"]` · **وصامتٌ ⟵ `NULL`**. وعلى **جهازٍ آخر** (طلبٌ بلا كوكي إطلاقًا):
  `ads=false/true/false/false` و`analytics=false/true/true/false` على التوالي —
  وكلّها كانت `NULL` قبل الإصلاح.
  الحارس: `tests/Feature/Growth/ConsentTravelsToTheAccountTest.php` — وثلاث طفرات
  تُسقِطه (نزع نداء `adopt()` · غياب الكوكي يُكتَب قبولًا · دهس اختيار الحساب).

**مُثبَت شغّالًا (لا يُعاد فحصه):** سلسلة الاكتساب الأربع وصلات (زيارة موسومة ⟵
موافقة «قياس داخليّ» ⟵ تسجيل ⟵ اعتماد إداريّ ⟵ شراء) — والرحلة المقابلة بالرفض
تُخرِج أعمدةً فارغة وصفرَ صفوفٍ في اللوحة · بار «أكمل ملفك» ⟵ 100% ⟵ **3 تذاكر مرّة
واحدة** (ثلاث زيارات متتالية · سطر دفتر واحد) · `sitemap.xml` يضمّ التدريبات
والمسارات والشهادات ويلتقط المقال المنشور فور نشره · حاجز المعاينة في الخادم
(الدرس 20 يفتح 200 · و21–24 يردّون 403) · وسم UTM على روابط حزمة المتطوّعين.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** د-8 **مقفولة** — سلسلة الاكتساب (21.2-ح) متّصلة من الزيارة إلى الشراء،
  وغرض «قياس داخليّ» (`analytics`) صار له قارئٌ حقيقيّ بعد أن كان بلا نداءٍ واحد.
- **آخر نقطة وصلنا لها:** ثلاث رحلات HTTP على `127.0.0.1:8213` (موافقة قياس ·
  رفض · إعلانٌ بلا قياس) بقراءةٍ من القاعدة عند كلّ خطوة، وعشر طفراتٍ سقطت كلّها.
  وأُعيدت الإعدادات إلى حالتها قبل الرحلة (`ads.tracking.enabled=0` · بكسل فارغ).
- **الخطوة الجاية:** (1) `robots.txt` يُخدَم من المتحكّم لا من ملفّ ثابت ·
  (2) `growth.invite_board.enabled` · (3) `SafeHtml::clean()` يحذف نصّ الوسم معه.

- **دفعة البقايا الستّ (2026-08-04):** أُغلِقت **موافقة الزائر ⟵ حسابه** (21.3-د).
  واللمسة على `app/Services/Ads/Consent.php` **ميثودٌ واحد جديد** (`adopt`) بلا مساسٍ
  بأيّ ميثودٍ قائم — والمجال مزدحمٌ بإيجنتٍ آخر. والثلاث الأخرى في هذه الوثيقة
  (`robots.txt` · `invite_board.enabled` · `SafeHtml`) **لم تُلمَس** وتبقى كما هي.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `app/Http/Controllers/Growth` · `resources/views/growth` · `routes/parts` · `tests/Feature/Growth`
- **الاختبارات:** 11 ملفّ Feature في `tests/Feature/Growth` — شغّلها بـ`php artisan test tests/Feature/Growth`.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `app/Services/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-04 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-04 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
