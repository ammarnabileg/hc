# 🗂️ `app/Services/Certificates` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** إصدار الشهادات والتحقّق العامّ منها (8).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.5-ج · 2.7 · 2.10.1 · 2.14-3 · 2.16 · 8.1 · 12.5-هـ · 12.5-ج · 12.5-ب · 13.4-ق
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (6):**
  - `ArabicText.php` — تشكيل النصّ العربيّ للرسم بـGD (8 · 12.5-ب).
  - `CertificateIssuer.php` — إصدار الشهادات (8 · 12.5-ج): كود فريد + توقيع رقميّ (Hash)
  - `CertificateNumber.php` — نظام الترقيم المخصّص لكلّ نوع/اعتماد: بادئة + سنة + تسلسل (12.5-ب)
  - `CertificateRenderer.php` — مولّد صورة الشهادة على الخادم بـGD (8 · 12.5-ب) — **بلا أيّ خدمة أو مكتبة خارجيّة**.
  - `CertificateSignature.php` — 🔏 التوقيع الرقميّ للشهادة — **مصدرٌ واحد للاشتقاق والتحقّق** (8 · 8.1 · 12.5-هـ).
  - `QrCode.php` — مولّد QR مكتوب هنا بالكامل (8.1 · 12.5-هـ) — **بلا أيّ مكتبة خارجيّة** لأنّ الشبكة محجوبة.
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**أ-4 — التوقيع الرقميّ صار ضمانةً لا دعوى (2026-08-03):**

- ✅ **مصدرٌ واحد للتوقيع:** `CertificateSignature` — `hash_hmac('sha256', …, app.key)` على حمولةٍ قانونيّة الترتيب: `الإصدار|الكود|صاحبها|مفتاح النوع|لحظة الإصدار UTC|بصمة data_snapshot|بصمة template_snapshot`. و`CertificateIssuer` يوقّع منه بعد اكتمال اللقطتين — لا نسخةَ ثانية في المشروع.
- ✅ **الحقل `hash` صار له قارئ:** `verdict()`/`matches()` بـ`hash_equals` (مقارنةٌ آمنة زمنيًّا)، وصفحة التحقّق تستهلكه فعلًا. وثلاث نتائج لا اثنتان: `match` · `mismatch` · `unsigned`.
- ✅ **التوقيع يغطّي ما يُعرَض:** تبديل اسم الحائز في اللقطة المجمَّدة يكسر التوقيع. والحالة (سارية/منتهية/ملغاة) **خارج** الحمولة عمدًا — تغيّرها مشروع بعد الإصدار (13.4-ق) ولا يجوز أن يجعل الشهادة «مزوَّرة».
- ✅ **التوقيع بلا مفتاح أُزيل من آخر موضعٍ فيه:** `Admin/Volunteer/CertificateEligibility::issueDirectly()` حُذف بالكامل (كودٌ عشوائيّ + بلا `template_snapshot` + `sha256` مكشوفة + ابتلاعُ كلّ استثناء) — والمسار الوحيد الآن `CertificateIssuer`، والتعثّر يُعلَن بسببه من `setting('volunteer_cert.issuer_unavailable')`.
- ✅ **الشهادات القائمة:** مايجريشن `2026_08_03_120000_certificates_seal_existing_signatures` يختمها **مرّةً واحدة** بخطّ أساسٍ مُعلَن (`backfill()`)، فلا تظهر شهادةٌ صحيحة بوسم «التوقيع لا يطابق». وما يُكتَب بعده لا يمرّ إلّا موقَّعًا.
- ✅ **مغلَق ومُثبَت:** كاش صورة الشهادة **ليس بمفتاح الكود وحده** — `CertificateRenderer::path()` يحمل بصمة `data_snapshot + template_snapshot + status + hash`.
- 🧪 **الاختبارات:** `tests/Feature/Certificates/CertificateSignatureTest.php` (8) و`VolunteerCertificateIssuePathTest.php` (4).
- ⬜ **متبقٍّ (خارج أ-4):** `app.key` الفارغ يجعل كلّ الشهادات «لا تطابق» — سلوكٌ مقصود وظاهر، لكن يستحسن حارس تنصيبٍ يمنع الوصول لهذه الحالة أصلًا.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** أ-4 مُصلَحة ومُختبَرة (2026-08-03) — بلا commit.
- **آخر نقطة وصلنا لها:** طفرةٌ لكلّ اختبار: إعادة زرع التوقيع بلا مفتاح · إبطال `hash_equals` · إعادة `issueDirectly` · نزع التحقّق من الصفحة — وكلّها أسقطت اختباراتها.
- **الخطوة الجاية:** بنود الأوديت الأخرى (خلفيّة الخريطة المنقّطة وأيقونة الشارة في 8.1) خارج نطاق أ-4.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `resources/views/certificates` · `routes/parts` · `tests/Feature/Certificates`
- **الاختبارات:** 4 ملفّ Feature في `tests/Feature/Certificates` — شغّلها بـ`php artisan test tests/Feature/Certificates`.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `app/Services/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-03 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-03 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
