# 🗂️ `app/Services/Events` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** الفعاليّات والحضور (11).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.8 · 2.13-ب · 2.13 · 2.15-أ-4 · 2.16-ج · 2.17-أ · 12.5-ب · 12.5-ج · 12.5-هـ · 12.5-د · 12.5 · 12.11
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (12):**
  - `AttendanceService.php` — إثبات الحضور (13.3): **كود OTP رقميّ** للأونلاين و**تشيك-إن** للأوفلاين،
  - `CertificateBridge.php` — جسر شهادة الفعاليّة (8 · 12.5 · 13.3).
  - `CheckinQr.php` — **تشيك-إن QR الديناميكيّ** (13.3 · 12.11 · 24.3).
  - `EventPresenter.php` — حالة الفعاليّة كما يراها مستخدمٌ بعينه (13.3 · 24.5):
  - `EventQuery.php` — فلاتر شاشة الفعاليّات (24.5): **ثلاثة ظاهرة** (النوع · الفترة · التصنيف) + بحث،
  - `IcsGenerator.php` — «أضِف لتقويمي» (13.3) — ملفّ ICS مكتوب بأيدينا سطرًا سطرًا، **بلا أيّ مكتبة تقويم**.
  - `LedgerBridge.php` — جسر المحفظة (القسم 19).
  - `QrMatrix.php` — مُرمِّز QR مكتوب بأيدينا سطرًا سطرًا — **بلا أيّ مكتبة خارجيّة وبلا أصلٍ من
  - `RegistrationService.php` — التسجيل في الفعاليّة (13.3): مجّانًا أو بالكوينز/التذاكر،
  - `ReminderScheduler.php` — **تذكيرات الفعاليّات المجدولة** (13.3 · 12.11 · 24.3).
  - `ShareCardRenderer.php` — صورة OG لكلّ رابط فعاليّة (21.1-أ) وبطاقة التذكرة القابلة للنشر (13.3) —
  - `Tracker.php` — تتبّع خادميّ خفيف (21.3) — بلا أيّ طرف ثالث وبإزالة تكرار عبر event_uid
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
### ⬜ نصٌّ محروق باقٍ في هذا المجلّد — بسببه مكتوبًا (دفعة `app/Services` · 2.13)
- ✅ **[مقفولة 2026-08-05] `EventQuery::MODES`/`PERIODS` صارا `modes()`/`periods()`.** 3 عناوين نوع + 5 عناوين فترة صارت من `setting()`، والمفاتيح الداخليّة بقيت `MODE_KEYS`/`PERIOD_KEYS`. حُدِّثت مواضع القراءة في `EventController` (فلترة) وقالب `events/index`. المفاتيح في `EventDemoSeeder`. **الدليل:** `php artisan test --filter="EventQuery|EventController|Events"` (69) قبل/بعد بلا فرق.
- ✅ **[مقفولة 2026-09-09] فجوة `settings:coverage --dead`: `events.certificate.code_prefix` كان وعدًا كاذبًا — شُطِب لا وُصِل.**
  تتبّعتُ التوليد الفعليّ: `CertificateBridge` ⟵ `CertificateIssuer::issue()`
  ⟵ `CertificateNumber::next()` (`app/Services/Certificates/CertificateNumber.php:27`)
  يقرأ `$type->numbering_prefix` — عمودٌ على `CertificateType` نفسه، مضبوطٌ
  لكلّ نوع شهادةٍ من شاشة إدارة الشهادات
  (`admin/certificates/partials/types.blade.php:92`، «بادئة الترقيم») — لا
  من `events.certificate.code_prefix` بالمرّة؛ الحرق الفعليّ في
  `CoreSeeder::certificateTypes()` (~سطر 299) كان في العمود لا في الكود
  المولِّد. ولا وصلَ ممكنٌ أصلًا: `CoreSeeder` يزرع `numbering_prefix` ضمن
  `DatabaseSeeder` **قبل** `SettingDefinitionsSeeder` (الذي يزرع هذا المفتاح
  عبر اكتشاف `EventDemoSeeder::settings()`)، فقراءته من هناك كانت سترتدّ
  لنفس الافتراضيّ بلا أثر. والعمود أدقّ من مفتاحٍ عامٍّ واحد أصلًا: بادئةٌ
  مستقلّة لكلّ نوعٍ من الأنواع الثمانية (12.5-ب)، لا بادئة فعاليّاتٍ واحدة.
  **الفعل:** حُذف الصفّ من `EventDemoSeeder` وشُطب بمايجريشن
  `2026_09_10_100090_a_certificate_prefix_setting_was_a_dead_promise.php`
  (نمط الفئة (3) من `2026_09_04_100010_dead_flags_from_an_aborted_first_pass_get_swept`).
  **الدليل:** `php artisan test --filter=AdminSystemSettingsCoverageTest` (10/10) +
  `php artisan test --filter=Events` (82/82) — و`php artisan settings:coverage --dead`
  بعد `migrate:fresh --seed` لم يعد يذكر `events.certificate.code_prefix` ولا
  مجموعة `events` بالمرّة. Mutation-tested: إعادة صفّ السيدر أسقطت
  `test_swept_dead_orphans_do_not_come_back` بالضبط كما يُتوقَّع، ثمّ اُستُعيد.


- ✅ **13.3 · 12.11: تشيك-إن QR — مبنيّ ومُثبَت (2026-08-31).** مُرمِّز QR مكتوب
  بأيدينا (`QrMatrix`) **بلا أيّ مكتبة**، ومصفوفتُه مطابقةٌ بايتًا ببايت لمرمّعٍ
  مرجعيّ مستقلّ في الإصدارات 1–10 عند حدود السعة. والرمز **ديناميكيّ** يتجدّد كلّ
  30ث (24.3) و**مربوطٌ بصاحبه** بتوقيع HMAC يشمل رقم تسجيله — فتبديل صاحب الرمز
  يُرَدّ، وهو عين «يمنع استخدام كود شخص لآخر» (12.11). الرحلة مُثبَتة بطلبات HTTP:
  توليد ⟵ مسح ⟵ `attended=1` + 200XP + تذكرة + شهادة **من دفتر الأستاذ** ⟵
  إعادة المسح تُرَدّ بلا صرفٍ ثانٍ · والمزوَّر والمنتهي يُرَدّان بلا أثرٍ في القاعدة.
- ✅ **[مقفولة 2026-09-10] 13.3 · 24.3-سطر-5040: «أفاتارات المسجّلين» (الدليل
  الاجتماعيّ) صارت مبنيّة خلف Toggle إداريّ حقيقيّ.** كان المعروض عددًا فقط
  («1 مسجَّل · باقي 39 مكان»)، و`x-avatar` في `show.blade.php` للمتحدّثين
  وحدهم. `EventController::show()` صار يحمّل `registrations()->with('user')`
  (بسقفٍ إعداده `events.show.avatars_limit`، افتراضه 8، مع عدّاد «+N» للباقي)
  خلف `events.show.registrant_avatars_enabled` — تمامًا كما ينصّ الهيدر
  «Toggle أفاتارات المسجّلين». **الدليل:**
  `tests/Feature/Events/EventsRegistrationTest.php` (+3 اختبارات: تظهر
  افتراضيًّا · تختفي كليًّا والـToggle مقفول · السقف يعمل مع عدّاد الفائض) —
  Mutation-tested: حذف شرط الـToggle، حذف السقف، حذف شرط عدّاد الفائض —
  الثلاثة أسقطت اختبارها المقابل.
- 🔍 **13.3: «لايف/شات» للفعاليّة الأونلاين — أُعيد فحصها 2026-09-10،
  غامضةٌ لا فجوةٌ مؤكَّدة.** العبارة الوحيدة في كامل الدستور (سطر 2762):
  «أونلاين: رابط الانضمام (يوتيوب لايف / لينكدإن / …) يظهر قبل الموعد
  بفترة + لايف/شات» — بلا Toggle إداريّ ولا صلاحيّة ولا أيّ ذكرٍ آخر في
  الشاشة الإداريّة (24.3)، خلافًا الصارخ لكلّ ميزةٍ حقيقيّة أخرى في هذا
  القسم (كود الحضور OTP، المكافأة المتدرّجة، الأفاتارات أعلاه — كلّها
  موصوفةٌ بتفصيلٍ إداريّ). الأرجح أنّ «لايف/شات» يصف ما توفّره المنصّات
  الخارجيّة المذكورة (يوتيوب لايف/لينكدإن) بنفسها — لا ميزة شات/بثّ داخل
  التطبيق. **قرارٌ منتجيّ لا فجوة كودٍ يُبنى بلا حسم أوضح من صاحب النصّ.**
- ✅ مُثبَت تشغيليًّا: العرض المزدوج (كروت/تقويم) · بطاقة الفعاليّة بشارة النوع
  والعدّاد · الفلاتر (نوع/فترة/تصنيف/سعر/تسجيلاتي) · التفاصيل بأجندة ومتحدّثين
  وخريطة OSM للأوفلاين · اختيار نمط الحضور للهجين · التوقيت المحلّي و`calendar.ics`
  بمنبّه · التسجيل المدفوع يخصم من الدفتر (تذكرة واحدة) · **كود الحضور**: كود خاطئ
  يُرفَض، وصحيح ⇒ `attended=true` + 200 XP + 1 تذكرة + شهادة `EVT-2026-000001`،
  والإعادة لا تصرف مرّة ثانية · المكافأة المتدرّجة معروضة بعدّادها.
- ✅ **13.3 · 12.11 · 24.3: التذكيرات المجدولة — مبنيّة ومُثبَتة (2026-08-31).**
  `ReminderScheduler` + الأمر `events:remind` + جدولتُه **كلّ خمس دقائق** في
  `routes/console.php`. المواعيد قائمةٌ في الإعدادات (افتراضيًّا: قبل يوم · قبل
  ساعة)، والقنوات جرس + بريد، والمستلِم **المسجّلون**. وحارس عدم التكرار **فهرسٌ
  فريد في القاعدة** (فعاليّة · مستخدم · موعد · قناة) — تشغيلتان متتاليتان: 2 مُرسَل
  ثمّ 2 متخطًّى، بلا إشعارٍ ولا بريدٍ مكرّر.
- ⚠️ **قناة «Toast» في نصّ 24.3 لم تُبنَ قناةَ تسليمٍ مستقلّة** — فليس لها في هذا
  المشروع مستقرٌّ يلتقطه أمرٌ خلفيّ (هي عرضٌ لحظيّ لصفّ الجرس عند أوّل طلب،
  وطبقتُها مشتركة لا يملكها هذا المجال). سُجِّلت هنا بدل أن تُكتَب قناةً تدّعي
  التسليم ولا تسلّم.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->

- **الحالة:** أربع فجوات في مجال الفعاليّات أُغلِقت: تشيك-إن QR · تذكيرات
  مجدولة بأمرٍ وجدولة · شاشة «المسجّلون والحضور» الجامعة (2026-08-31) ·
  أفاتارات المسجّلين (2026-09-10). و«لايف/شات» أُعيد تصنيفها قرارًا منتجيًّا
  غامضًا لا فجوة كودٍ — راجع البند أعلاه.
- **آخر نقطة وصلنا لها:** `EventController::show()` يحمّل أفاتارات المسجّلين
  خلف Toggle + سقف إعداده، +3 اختبارات، 3 طفرات قُتِلت.
- **الخطوة الجاية:** قناة Toast للتذكير (مُسجَّلة أعلاه غيرَ قابلة للبناء
  بمعناها الحرفيّ). لا خطوة تنفيذيّة أخرى معلَّقة في هذا المجال حاليًّا.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `resources/views/events` · `routes/parts` · `tests/Feature/Events`
- **الاختبارات:** 10 ملفّ Feature في `tests/Feature/Events` — شغّلها بـ`php artisan test tests/Feature/Events`.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `app/Services/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-06 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-05 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
