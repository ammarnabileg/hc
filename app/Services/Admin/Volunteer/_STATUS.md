# 🗂️ `app/Services/Admin/Volunteer` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** «Volunteer» ضمن: خدمات لوحة الإدارة بمجالاتها (12).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.13-و · 2.13-هـ · 2.13 · 2.14-ب · 2.14 · 2.15-أ-3 · 2.15-أ-7 · 2.17-ب · 7.1 · 7.2 · 7.3 · 7.6
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (12):**
  - `AuditTrail.php` — سجلّ التدقيق لكلّ تغيير حسّاس في لوحة إدارة التطوّع (2.13-و).
  - `BehaviorLedger.php` — معاملة السلوك اليدويّة (13.4-ن-هـ) — المنفذ الوحيد لتقدير بشريّ على Rep،
  - `CapacityReport.php` — سعة الأقسام (13.4-ف) — **مؤشّرات لا موانع**: لا تُوقِف تسكينًا ولا ترقيةً.
  - `CertificateEligibility.php` — شهادات التطوّع (13.4-ع).
  - `Integrations.php` — نقاط التكامل: دفتر الأستاذ والإشعارات.
  - `OffboardingService.php` — الأوفبوردنج (13.4-س) — ثلاثة أنواع لا رابع لها:
  - `RepRuleWriter.php` — ⭐ كلّ قيم `rep_rules` تُعدَّل من شاشة ضبط Rep (13.4-ك · 13.4-ن).
  - `RewardGrantService.php` — إدارة المكافآت (12.9): منح/خصم رصيد لكودٍ واحد أو مئات دفعةً واحدة.
  - `SettingsCatalog.php` — كتالوج إعدادات مجال «إدارة التطوّع والتلعيب والمكافآت والفعاليّات».
  - `SettingsWriter.php` — كاتب الإعدادات: يكتب في `settings` ويمسح الكاش فورًا فينعكس التعديل
  - `VolunteerAnalytics.php` — تحليلات التطوّع — **مادّة قرار لا أرقام زينة**:
  - `WarSettingsService.php` — إعدادات الحروب (12.10-ج).
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**أوديت 12.9 · 12.10-ج — 2026-08-03 (ما يخصّ المرحلة 6):**

- ❌ **`RewardGrantService`: بطاقة المستلِم ليست صورة** — زرّ «حفظ الصورة» ينادي `window.print()`، والأفاتار غائب عن البطاقة (12.9 · 12.14-و).
- **مُثبَت شغّالًا — مصدر الرصيد:** `RewardGrantService::execute` لا يكتب في المحفظة مباشرةً؛ يمرّ عبر `Integrations::post` ⟵ `LedgerService` وحده. منحةٌ حقيقيّة أنتجت **قيدَين في `transactions`** بـ`source=admin` و`created_by` المالك و`balance_after`، **ومجموع XP ارتفع بالقدر الممنوح تمامًا (3411⟵3461)** — فلا سكَّ خارج دفتر الأستاذ. ومعه قيد `manual_rewards.execute` في التدقيق.
- **مُثبَت أيضًا:** المعاينة قبل التنفيذ بالرصيد قبل/بعد · استبعاد الخطأ والمكرّر بتنبيه · الخصم ينزل تحت الصفر · الشرائح والدفعات · `WarSettingsService` بقواعدٍ عامّة وOverride وReset.
- **لم أفحصه بالتشغيل:** «قفل الإعدادات أثناء حرب نشطة» في `WarSettingsService` — لم أُشغِّل حربًا حيّة.

**أ-4 — شهادات التطوّع دخلت مسار الإصدار المعتمَد (2026-08-03):**

- ✅ **`CertificateEligibility::issueDirectly()` حُذف بالكامل:** كان يكتب صفَّ شهادةٍ بيده كلّما تعثّر المُصدِر — كودٌ عشوائيّ `VPS-XXXXXXXXXX` خارج الترقيم (12.5-ب)، وبلا `template_snapshot` (12.5-ج)، وتوقيعٌ **بلا `app.key`**: `sha256(code|user|position|entity)`. والمسار الوحيد الآن `CertificateIssuer` (نفس معالجة `Services/Events/CertificateBridge`).
- ✅ **ولا ابتلاعَ للاستثناءات:** `try/catch (Throwable) { return null; }` أُزيل — الفشل يظهر ولا يُخبَّأ خلف وثيقةٍ أضعف، ويُعلَن بسببٍ من `setting('volunteer_cert.issuer_unavailable')`.
- ✅ **شهادة الخبرة:** إنهاء الشهادة السارية صار **بعد** نجاح إصدار البديلة لا قبله — فلا يبقى المتطوّع بلا شهادةٍ سارية بسبب تعثّرٍ عندنا.
- 🧪 **الاختبار:** `tests/Feature/Certificates/VolunteerCertificateIssuePathTest.php`.

- ✅ **[د-6 · 2026-08-03] `Integrations::post()` صار يختم كيان الحركة على صفّها.**
  «وكلّ حدث **موسوم بكيانه** في سجلّ المعاملات» (23-0.2-عضويّات-2) — وعليه وحده
  يتحدّد صاحبُ التزام التواصل عند −8: «أبلاين العضويّة التي وقعت فيها **المعاملة
  الكاسرة**» (23-0.2-1). والاشتقاق من المرجع وحده: `entity_id` مباشرةً، أو
  `membership_id` (وهو ما تحمله `BehaviorTransaction`) ⟵ كيان تلك العضويّة —
  وما لا مرجع له يبقى بلا كيان ولا نخترع له واحدًا.
  وقبله كانت **معاملات السلوك** — وهي أكثر ما يُنزِل الدرجة — تصل بلا كيان،
  فيقع الالتزام على أبلاين العضويّة الأساسيّة أيًّا كان مكان المخالفة.
  الحارس: طفرةُ حذف الختم أسقطت
  `WarningRungTest::test_the_duty_falls_on_the_upline_of_the_breaking_membership`.
- ⟵ ونداءُ سلّم العتبات من هنا صار `RepLadder::afterRepMovement()` (المدخل الواحد
  للدرجات الثلاث بترتيب النصّ) بدل نداء الدرجة الوسطى وحدها.
- ✅ **[د-6 · 2026-08-03] الأوفبوردنج صار يُنهي العضويّة المعلَّقة كما يُنهي النشِطة.**
  «ويعود التفويض تلقائيًّا عند إعادة التفعيل، **أو يتحوّل شغورًا حقيقيًّا (سلّم
  الترقية) عند قرار الإقصاء**» (23-0.2-4). وليس تفصيلًا: **الإقصاء لا يقع إلّا على
  معلَّق** — التعليق يسبق «أيّ إنهاء» (23-0.2-4) والإقصاء يُرفَض لمن لم يبلغ العتبة
  (13.4-س-أ). فاقتصارُ `complete()` على `status = active` كان يعني أنّ **المُقصى
  يخرج وعضويّاته `suspended` إلى الأبد**: دورُه في يده، وحلقتُه قائمة في سلسلة
  التصعيد، ولا شغور يُملأ. صارت `CLOSABLE_STATUSES = ['active','suspended']`،
  ويُقفَل معها صفّ التعليق بسبب `dismissal`.
  الحارس: `SuspensionTest::test_dismissal_ends_the_suspended_memberships_and_turns_the_cover_into_a_real_vacancy`
  — وطفرةُ إرجاع `['active']` أسقطته.
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** أوديت فقط — بلا إصلاح.
- **آخر نقطة وصلنا لها:** نُفِّذت منحةٌ حقيقيّة وتُتُبِّع أثرها في دفتر الأستاذ والتدقيق والإشعارات.
- **الخطوة الجاية:** فحص قفل إعدادات الحروب بحربٍ حيّة.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `app/Services/Admin` · `app/Http/Controllers/Admin` · `resources/views/admin` · `routes/parts` · `tests/Feature/Admin`
- **الاختبارات:** 1 ملفّ Feature في `tests/Feature/Admin` — شغّلها بـ`php artisan test tests/Feature/Admin`.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `app/Services/Admin/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-03 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-03 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
