# 🗂️ `app/Services/Volunteer/Meetings` — حالة المجلّد

> وثيقة **القاعدة 2.12** (توثيق التقدّم داخل كلّ مجلّد).
> الأقسام المحصورة بين علامتَي `تلقائيّ:بداية` و`تلقائيّ:نهاية` يعيد كتابتها
> `php artisan docs:status` من الكود نفسه — فلا تُحرّرها بيدك.
> أمّا **المتبقّي** و**الجاري الآن** فبيدك أنت، ولا يمسّهما المولِّد أبدًا:
> حدّثهما **قبل كلّ جلسة عمل وبعدها** كما تنصّ القاعدة.

## 🎯 الغرض/المطلوب
<!-- تلقائيّ:بداية:الغرض -->
- **الوظيفة:** «Meetings» ضمن: التطوّع بكلّ طبقاته: المهام والاجتماعات والأداء والتقدير (13.4).
- **البنود الحاكمة (من تعليقات الكود نفسه):** 2.13 · 2.16 · 2.17-ب · 13.4-ن-ب · 13.4-ن · 13.4-ط · 13.4-ن-هـ · 13.4-ح · 24.4
- **قواعد سارية على كلّ ما هنا:** الصلاحيّة على كلّ مسار (12.2.1) · المحظور يُخفى لا يُعطَّل (2.15-أ-7) · لا رقم ولا نصّ محروق (2.13) · 🔒 الماليّات لمالك المنصّة وحده (12.7).
<!-- تلقائيّ:نهاية:الغرض -->

## ✅ المُنجَز
<!-- تلقائيّ:بداية:المنجز -->
- **أصناف (3):**
  - `AttendanceService.php` — تسجيل الحضور وقيمته على درجة الالتزام (13.4-ن-ب · 24.4).
  - `MeetingLedger.php` — غلاف آمن حول دفتر الأستاذ وبوّابة الإشعارات.
  - `MeetingScope.php` — نطاق الاجتماع: مَن جمهوره، ومَن يملك إدارته (13.4-ح · 13.4-ن-ب).
<!-- تلقائيّ:نهاية:المنجز -->

## ⬜ المتبقّي
<!-- بيدك:بداية:المتبقّي -->
**نتيجة أوديت المرحلة 11 (الدستور 23) — 2026-08-03:**

- ✅ الاجتماع **فعاليّة موثّقة بكود حضور** لا نوع مهمّة — متّسق مع 23-0.3
  («~~اجتماع~~ ممنوع كنوع، والبديل الفعاليّات بكود الحضور»):
  `AttendanceService::register()` يفحص `meetings.attendance_code`، وشاشة
  `/volunteer/meetings` تُحمَّل 200 وتعرض «كود حضور».
- ✅ **[مقفولة 2026-09-10] ميتينج لجنة التحقيق الثلاثيّ صار فعاليّةً
  حقيقيّة على هذا النظام بالذات — لا نظامًا موازيًا.** كان
  `InvestigationCase.meeting_scheduled_at` طابعًا زمنيًّا مجرَّدًا بلا صفّ
  `meetings` حقيقيّ (التفصيل الكامل في
  `app/Services/Volunteer/Retention/_STATUS.md`). أُضيفت قيمة جمهورٍ جديدة
  `audience='specific'` هنا في `MeetingScope::isAudience()`/
  `audienceUserIds()`/`visibleQuery()` (وجدول `meeting_invitees` الجديد) —
  **إضافيّةٌ بحتة**: الثلاث قيمٍ الأصليّة (`entity`/`sub_entity`/`all`) لا
  تلمسها، و`MeetingController::store` يرفض غيرها فلا اجتماعٍ عاديّ يمرّ
  بالقيمة الجديدة. جمهورها اسميّ (مستخدمون محدَّدون بلا كيان) — أوّل استخدامٍ
  من نوعه لهذا الحقل، وقد يُعاد استعماله لاحقًا لأيّ اجتماعٍ آخر يحتاج
  جمهورًا اسميًّا بدل كيانيّ.
  **الدليل هنا تحديدًا:** طفرة `audienceUserIds()` كشفت أنّ حذف الفرع
  الجديد كان سيجعل `AttendanceService::end()` يُخطر **كلّ عضوٍ نشِط في
  المنصّة** لا الجمهور الاسميّ (لأنّ `entity_id` فارغٌ لهذا النوع فيسقط شرط
  الفلترة القائم كلّه) — خطرٌ حقيقيّ لا نظريّ، مُثبَتٌ بالطفرة لا افتراضًا.
- ⬜ **لم أفحصه:** توليد الاجتماع مهمّةَ «تنفيذ» لبنود المحضر (23-0.3).
<!-- بيدك:نهاية:المتبقّي -->

## 🔄 الجاري الآن
<!-- بيدك:بداية:الجاري -->
- **الحالة:** ميتينج لجنة التحقيق (البند الوحيد المفتوح من أوديت المرحلة 11
  هنا) صار فعاليّةً حقيقيّة على `MeetingScope`/`AttendanceService` القائمتين
  — بإضافة جمهورٍ اسميّ (`audience='specific'`) لا تمسّ الأنواع الثلاثة
  الأصليّة.
- **آخر نقطة وصلنا لها:** 3 فروعٍ جديدة في `MeetingScope` + جدول
  `meeting_invitees` — 4 طفرات قُتِلت هنا (`isAudience`/`audienceUserIds`
  الفرعان الجديدان · فرع `visibleQuery` الجديد)، أخطرها طفرة
  `audienceUserIds` (راجع الملاحظة أعلاه).
- **الخطوة الجاية:** لا خطوة معلَّقة هنا — المجلّد مستقرّ.
<!-- بيدك:نهاية:الجاري -->

## 🔗 التبعيّات والملفّات المهمّة
<!-- تلقائيّ:بداية:التبعيات -->
- **الطبقات الأخرى لنفس المجال:** `app/Services/Volunteer` · `app/Http/Controllers/Volunteer` · `resources/views/volunteer` · `routes/parts` · `tests/Feature/Volunteer`
- **الاختبارات:** مافيش مجلّد اختبارات بنفس اسم المجال — راجع `tests/Feature` قبل ما تفتح ملفًّا جديدًا.
- **المرجع الحاكم:** `دستور اساسي.md` · **وكيف نكتب:** `docs/BUILD.md`.
- **المجلّد الأب:** `app/Services/Volunteer/_STATUS.md`.
<!-- تلقائيّ:نهاية:التبعيات -->

## 🕒 آخر تحديث
<!-- تلقائيّ:بداية:التحديث -->
- **آخر توليد لهذه الوثيقة:** 2026-08-02 — `php artisan docs:status`.
- **آخر لمسة للمجلّد:** 2026-08-02 — Claude.
<!-- تلقائيّ:نهاية:التحديث -->
